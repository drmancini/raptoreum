<!-- lifecycle: living — revised in place, always current
     owns:      the schedule: what gets built, in what order, effort, design certainty, the gates
     not mine:  the design (transaction-decoupling.md), values (findings.md), measurements (perf-results.md)
     known debt: this file carries dated progress notes and measured figures, which its own
                class forbids. They belong in the log and in findings; see check-numbers.py. -->

# Build plan — transaction decoupling

**Version 1, 17 September 2026.** Owns the **schedule**: what gets built, in what order, how
long, and which parts are unknown. The design itself lives in `transaction-decoupling.md` (v8);
the measurements behind every figure are in `perf-results.md`; constants are cited from
`perf-constants.md`. Where this document and §13 of the design disagree about order or effort,
this one wins.

## The design point

Commitments fill a 2-8 MB block every two minutes (D-4).

| | 2 MB of commitments | 8 MB of commitments | from |
|---|---|---|---|
| identifiers per block | 62,500 | 250,000 | K-1, 32 B each |
| throughput | **520 tx/s** | **2,083 tx/s** | D-4 |
| body bytes per block | 23 MB | 93 MB | F-30 × the count |
| body bandwidth | ~195 kB/s | ~780 kB/s | derived |
| vs today (2 MB of bodies) | 11.6× | 46× | K-1, F-30 |

**Peaks and bursts, not sustained load** (RTM team, relayed by the owner 2026-09-17). Capacity
is therefore sized for the burst and storage for the average — which is what keeps retention and
the state root out of this plan. Both assumptions carry tripwires; see the last section.

**One fork** (D-14). The format, the 8 MB `MaxBlockSize` (K-1) and the `MAX_PROTOCOL_MESSAGE_LENGTH` raise (K-7) all
ride the same activation, with 2 MB mined by policy afterwards (D-14). Splitting them would make the
higher target a second hard fork and a second ecosystem event for no benefit.

**The one genuine unknown, partly priced.** The acceptance layer. No production chain has a block
whose acceptance depends on a network fetch, so there was nothing to copy and phase 0 existed to
price it. **0.1 has run (2026-09-18): no kill**, with the cost signal the criterion anticipated —
one per-block bit and one validity rung (F-25k). The half that looked hardest, teaching chain
selection to hold a block it cannot complete and pick it up later, turned out to be largely built
already, because pruning needs the same thing (F-25f). But the probe reached that half by
withholding a block it had **already validated** (F-25e3), so the rung — the thing that makes a
commitment-only block honest — is untested, and it is what decides whether the probe's encoding
survives. What remains genuinely new: the rung, the persisted bit, the body store, and a fetch
layer that does not skip its own gaps (F-25e2).

**Effort.** 54-90 weeks of components; roughly 12-20 months of solo calendar time with phase 3
interleaved. Estimates include tests and were revised upward twice under adversarial review.

---

## Phase 0 — de-risk before committing · 6-8 weeks

Nothing in phase 1 starts before **0.1c** settles the rung. **Settled 2026-09-18** — the bit is settled and inseparable from the fetch layer (F-38, F-40); the rung sits after the commitment-checkable checks, and the gap R-28 exposed is **three** rules, not a category (F-43, F-44) — one of them `bad-txns-oversize`, the very cap §1A's body-byte argument leans on. 0.1 returned its verdict on 2026-09-18 — no kill — though it reached that verdict by withholding a block it had already validated (F-25e3), which is why the rung was settled by audit rather than probe. **The encoding is now decided too (D-16): no new ordinal.** `BLOCK_VALID_TRANSACTIONS` is granted as commitment-valid and the three uncovered rules move to the connect path, which avoids renumbering three persisted values and migrating every index entry on disk.

> **0.1's kill criterion, written down before it runs.** The acceptance layer is **not
> tractable**, and §15 becomes the design, if making any probe scenario safe requires
> **relaxing an invariant that today catches database corruption** — `CheckBlockIndex`'s
> `HAVE_DATA ⇒ nTx > 0 ⇒ VALID_TRANSACTIONS ⇒ candidacy` chain, or `ConnectTip`'s guarantee
> that a selected block is readable — rather than adding a state alongside it. Weakening one of
> those trades a liveness problem for a silent-corruption problem, which is a worse design than
> not decoupling.
>
> Two softer signals, neither a kill on its own: needing more than one status bit and one
> validity rung in the block index is a **cost** signal, to be re-estimated rather than
> abandoned; and failing to reach "converges once bodies arrive" within two weeks is evidence
> about the layer rather than about the probe.
>
> **Pass** is all scenarios reaching: never asserts, never marks the block invalid, never wedges,
> and always converges once the bodies arrive.

| # | Component | Effort | Design |
|---|---|---|---|
| 0.1 | **Acceptance probe. DONE 2026-09-18 — verdict: no kill, but the cost signal is one bit plus one rung.** Phase A measured the blast radius of a missing body: `VerifyDB` called it database corruption, serving aborted the process, `ConnectTip` hit `AbortNode`. Phase B moved the withholding from read time to **accept** time, which is the only way to reach the states that matter, and showed chain selection holding a block it cannot complete without condemning it, then picking it up when the rest arrives — through one arrival path, for a block that had been body-validated, under an encoding where `BLOCK_HAVE_DATA` means bodies held (F-25e3, F-25f). The pruning machinery carries most of that half. **Honest accounting**: of the six named scenarios, restart ran; peer churn, competing tip, ChainLock, reorg and `-reindex` did not. An adversarial review found nine further states and corrected three claims — see the 18 Sep correction entry in the log. The criterion is not met because a **per-block bit** is "a state alongside" (F-25k); the probe's global flag was a shortcut and is not the shape of the fix. | 3-5 d | **done** |
| 0.1c | **The rung probe + accept-path audit. DONE 2026-09-18.** Settled: `BLOCK_HAVE_BODIES = 256` with the commitment block stored normally costs **zero relaxed assertions** (F-38), and the bit **cannot ship without the fetch layer** (F-40). Retracted (R-28) and then resolved by audit rather than probe, since the harness validates bodies before withholding them and so cannot test a rung at all: of thirteen checks on the accept path, **six are answerable from a commitment block** and the rung sits there (F-43); of the body-dependent rest, all but **two** are already re-checked at connect (F-44). Two traps found on the way: `fChecked` must not survive body assembly or bodies are never checked (F-45), and a third byte-indexed constant compares a transaction count to a byte cap (F-46). | 1 w | **done** |
| 0.2 | **Characterisation tests.** Pin what the node accepts and rejects across normal and multi-input payments, every asset op, futures, every special-tx type, InstantSend interactions, and every rejection path. Tier 1 — strictly before decoupling. Runs on the rig host; mario's default Python 3.13 has no `asyncore` (**R-35, 2026-09-19: this only meant the DEFAULT interpreter — 3.11.9 is already installed via pyenv and runs the framework fine; `raptoreum/.python-version` now pins it locally**). **Three additions from 0.1's correction**, each a behaviour 1.3 will change and nothing else would catch: the equal-work tie-break (F-35), and the two arrival paths that today read `nTx != 0` as "had it, then pruned it" — unrequested blocks and compact-block relay (F-36). Characterise them *before* they are touched, or the regression is invisible. | 2-3 w | straightforward |
| 0.3 | **Two cheap measurements.** `spork show` and `getblock <tip> 2` on the mainnet node — sporks 2, 3 and 19 are compiled off and whether they are live decides four priced items below. And fix `bench_inputs.py` (size-scaled fee, non-overlapping UTXOs, ≤675-input ceiling) to calibrate 1.2's work unit. | 1-2 d | straightforward |
| 0.4 | **Smartnode swarm bring-up. DONE 2026-09-18.** Ten live smartnodes on twelve WAN hosts; `llmq_test` and `llmq_test_v17` both formed with 8/8 contributions and zero complaints; first ChainLock signed; InstantSend locking a transaction on submission. The figure: **~15 s per DKG phase over real latency** (F-48), against the ~1 s unpaced regtest mining allows — which is why three earlier attempts failed identically. Harness: `dkg-pace.py`. Five traps recorded (F-50, F-51, F-52) including that enabling sporks 2/3/19 before a quorum exists empties every block. **Every earlier swarm figure's upper-bound caveat can now be retired measurement by measurement** (F-49). | 2-3 w | **done** |

## Phase 1 — consensus surface, one fork · 15-31 weeks

| # | Component | Effort | Design |
|---|---|---|---|
| 1.1 | **Block format.** ~~Second `CBlock` serialization — coinbase in full, every other entry a bare 32-byte identifier. Selector is a stream flag negotiated by service bit, never a header bit.~~ **Retracted (F-80..F-82, R-28b) and superseded by what's actually built (E1, Fable review, 2026-09-19): a distinct `CCommitmentBlock` type** (`primitives/block.h` — a coinbase plus one 32-byte identifier per remaining transaction, not a second serialization of `CBlock`), with `CommitmentsFromBlock`/`MaterialiseBlock` converting between it and a real `CBlock` (materialisation produces a fresh object so `ConnectBlock`'s own `CheckBlock` cannot short-circuit on `fChecked`). Negotiated by a capability service bit (`NODE_COMMITMENTS`) plus an explicit `SENDCOMMITMENTS` handshake (F-85/R-31) — the retracted design's "stream flag negotiated by service bit" language conflated the capability negotiation (real, kept) with a stream-flag serialization selector (`SER_COMMITMENTS`, dead code, removed — F-121/F-119). `ReadCommitmentBlockFromDisk` is a real API **boundary** distinct from the materialising read (R-30) but does not yet implement a genuinely body-less read — that's phase 2's job, same as the per-request wire negotiation of which form to fetch (nothing yet delivers a real standalone `CCommitmentBlock` over the wire, F-105). | 1-2 w | **1.1 complete 2026-09-19** — format, capability bit and handshake built; the phase-2-scoped pieces (body-less disk read, per-request form negotiation) are explicitly out of scope here, not overlooked |
| 1.2 | **Block resource budget** (§1A). Sigop cap re-based to committed count and a consensus body-byte cap — the per-transaction work cap originally proposed here is **dropped** (D-17): it bounded nothing the sigop budget didn't already, and had no connect-time home without 1.3. **The sigop budget is built, set to 250,000 (D-18, R-33), and one implementation bug found and fixed post-build (F-95, HIGH — legacy overcounted a small created multisig output enough to stall the miner; a second adversarial pass on the tests themselves found and closed a tautology plus three coverage gaps, F-96)**: `GetAccurateSigOpCount`/`GetAccurateOwnSigOpCount` close F-86 (additive, not a replacement) and F-87 (`CHECKDATASIG`, opt-in, never editing the shared legacy counter); `g_commitmentBudgetActive` (test-only until 4.6) gates it per F-88, threaded through ATMP, `ConnectBlock`, both view-less legacy checks and the miner (F-89). **D-18's stated worst-case time was wrong twice and is now measured, not estimated** (F-91, F-93, F-94, F-97): the mempool-only `~O(n²)` input-count effect (F-93, up to ~370-385 ms) doesn't touch `ConnectBlock` for a zero-sigop shape (F-94, ~70 ms) — but redone with real sigops present, `ConnectBlock`'s own cold cost climbs from ~130-200 µs/sigop at low input count to a directly-measured **~320 µs/sigop at K-3's actual per-tx ceiling** (F-97, 6-rep median). Honest worst case: **~80 s, ~67% of the 120 s interval**, not the "~27%, tighter than today" first claimed. **The body-byte and input-count caps are now built too (D-19, F-98)**: sized to a 1,500 tx/s theoretical ceiling of *realistic* multi-input/output traffic (Mike's explicit direction, widening past the 2-in/2-out shape used for the sigop budget) — `COMMITMENT_BUDGET_BODY_BYTES = 110,000,000` and `COMMITMENT_BUDGET_MAX_INPUTS = 700,000`, two limits because a byte cap alone can't bound worst-case time (an attacker packing minimal ~41-byte inputs gets ~4.5× the input density realistic traffic ever needs). `GetBlockInputCount` enforces the input cap exactly at both view-less sites (`CheckBlock`, `ContextualCheckBlock`). **A second adversarial pass (F-99) found the miner-side approximation was worse than framed**: `CreateNewBlock`'s own `TestBlockValidity` throws on rejection, and `getblocktemplate`/`generateBlocks` just rebuild from the same mempool and hit it again — a deterministic stall under saturated-mempool traffic, not a one-off wasted block. Fixed: `addPackageTxs` now re-checks the exact ancestor-set aggregate before committing to a package. A parallel pass on the tests found real gaps (input-count boundary untested, byte cap only unit-tested in isolation, its gating not proven un-stuck-open) — three new tests close them, all mutation-checked. Full suite green except the pre-existing, confirmed-unrelated `miner_tests/CreateNewBlock_validity` failure. | 2-3 w | **1.2 complete** — sigop budget, body-byte cap and input-count cap all built, reviewed twice and hardened |
| 1.3 | **Acceptance layer.** Having the identifiers and having the bodies become two facts: a new status bit (a persisted block-index format change), a new validity rung, and the sites above `ConnectBlock` that read "do we have this block" — chain selection, candidate maintenance, `CheckBlockIndex`'s invariants, `NewPoWValidBlock`, pruning, `VerifyDB`, crash replay, `-reindex`, and the `InvalidateBlock` / `MarkConflictingBlock` / `EnforceBestChainLock` interactions. Plus a durable retry queue with per-block exponential backoff that never keys off accumulated work. **Re-estimated after 0.1**, and only for the half the probe reached: chain selection is largely built already, for pruning (F-25f). Everything else stands — the rung (which decides whether the probe's encoding survives at all, F-25e3), the persisted bit, out-of-order arrival (F-25j), the fetch cursor at `net_processing.cpp:FindNextBlocksToDownload` that jumps the gap (F-25e2), the unrequested and compact-block arrival paths (F-36), `-reindex`, ChainLock enforcement, and equal-work ties (F-35). **Started 2026-09-19, plan revised 2026-09-19 after a Fable adversarial review found a real encoding bug in the first draft (F-100) — broken into 7 ordered sub-steps** (below). | **8-16 w** | **more known than first thought** — `probe/acceptance-layer` (commits `a2d5f68c9`, `37a7c37a0`) already carries a reviewed, mostly-correct implementation of 1.3.1; the rung's own accept path (F-83) and the fetch layer's live-peer proof are not |
| 1.3.1 | **Rebase and harden `probe/acceptance-layer`'s per-block bit — do not reimplement from scratch.** That branch (marked "throwaway" only because the feature wasn't agreed yet — it now is) already has the correct **three-state encoding**: the commitment block is *always* written and `BLOCK_HAVE_DATA` stays honest; `BLOCK_HAVE_BODIES = 256` is set separately by a dedicated `ReceivedBlockBodies` writer, never by `ReceivedBlockTransactions` re-running (F-100 corrected the plan's first draft, which described a vacuous encoding where the bit degenerated to `HAVE_DATA` — that contradicted the very findings, F-38/F-40/F-41, measured against this branch). Also already there, and already found+fixed by an earlier review on that branch: the **fatal** out-of-order bug (`ReceivedBlockBodies` lacked `ReceivedBlockTransactions`'s parent guard, so a body arriving for a block whose parent was still header-only became a candidate and hit `FindMostWorkChain`'s **production** `assert(HaveTxsDownloaded())`); the already-stored-reorder fix (a still-withheld re-arrival must return before `SaveBlockToDisk`, or it double-writes and re-stamps `nSequenceId` for the whole subtree); `PruneOneBlockFile` clearing the bit with `HAVE_DATA`/`HAVE_UNDO`; dropping the false "on active chain ⇒ bodies" assert (pruning removes bodies from connected blocks); F-38c's `m_blocks_unlinked` recovery rule; `pindexFirstMissingBodies` beside `pindexFirstMissing`; and the minimal fetch-side fix (`ProcessHeadersMessage`'s direct-fetch walk gated on `HaveBodies()`, not `HaveTxsDownloaded()`/`HAVE_DATA` alone). **Correction (1.3.6, F-111, 2026-09-20): the row originally claimed this same treatment covered `FindNextBlocksToDownload`'s own cursor — it did not.** Reading the shipped code directly (not this row's own account of it) found the cursor advancement (`pindexLastCommonBlock`) was still gated on `HaveTxsDownloaded()` alone, F-25e2's exact bug, unfixed. Actually fixed in 1.3.6. **Still missing from the branch, to add here**: the CMPCTBLOCK handler's own `HAVE_DATA`-only gate (F-36's second site — mis-attributed to `FindNextBlocksToDownload` in the first plan draft; it is its own handler, checked before the `nTx` fallback); the serve side checking `HaveBodies()` before answering a `getdata` (absent from the probe); and a **migration gated by a persisted done-marker**, not an unconditional pass — `DB_FLAG "prunedblockfiles"` (`txdb.cpp`) is the in-tree precedent. Without the marker, a plain "`HAVE_DATA` ⇒ set `HAVE_BODIES`" pass run on *every* boot would re-mark genuine commitment-only blocks as bodies-held on the next restart after 1.3 ships. Add a **test-only runtime un-withhold** (an RPC, or an extension to `-perfwithhold*`) — today those flags parse once at init (`init.cpp:1642-1661`), which is why every convergence result in F-40/F-40b came from a **restart**, not a live two-peer session; 1.3.1 isn't provably done without a live (non-restart) convergence test. **Built 2026-09-19 (F-101).** The two commits cherry-picked cleanly across all 41 diverged commits (1.1, 1.2) with zero conflicts. Hardened: F-36's first site (`AcceptBlock`'s `nTx != 0` unrequested-drop) still needed its own fix beyond the probe's `fAlreadyHave` change — now discriminates pruned (`HAVE_DATA` clear) from commitment-only (`HAVE_DATA` set, bodies not); the CMPCTBLOCK handler's gate fixed the same way; the migration built with a persisted `"bodiesmigrated"` done-marker on the `"prunedblockfiles"` pattern, written before the flag so a crash mid-pass just retries. The serve-side gate needed no change — already present on `perf/throughput-rig` (not the probe branch) and upgraded automatically by `HaveBodies()`'s redefinition. New `acceptancebit_tests.cpp` (4 cases, mutation-tested) exercises the mechanism end to end via direct `ProcessNewBlock` calls, including a control proving pruning's own behaviour is unchanged. **Still open**: the live two-peer non-restart convergence test and the multi-peer out-of-order harness (1.3.3's job) — reasoned through manually so far, not yet proven live. Previously blocked on the functional Python framework being unrunnable here; **R-35 (2026-09-19) retracts that** — 3.11.9 is available via pyenv and runs the framework end to end, so 1.3.3 is unblocked, just not yet done. **A second adversarial pass (F-104, 2026-09-19) found a missed F-36-class site** (the `GETBLOCKTXN` handler had the identical `HAVE_DATA`-alone conflation, unguarded before an `assert()`'d disk read — a crash, not just a discard) **and a reindex gap in the migration** (`-reindex` never calls `LoadBlockIndexDB`, so the `bodiesmigrated` flag was never written on that path; fixed by writing it in `LoadBlockIndex`'s `needs_init` branch instead). Both fixed. The bodies-arrival test was also strengthened: it previously asserted only the status bits, not that the block actually became connectable or that no second copy was written — both now asserted and mutation-confirmed (deleting the candidate re-arm walk crashes the suite). | 2-3 w | **built, hardened, reviewed twice** — live-peer proof deferred to 1.3.3 |
| 1.3.2 | **The rung (D-16) and the commitment-block accept path (F-83) — one step, not two.** F-83, found independently twice and filed against 1.3, says the format has **no validation entry point at all**: `CCommitmentBlock::ComputeMerkleRoot()`/`HasDuplicateIdentifiers()` have zero production callers, `CheckBlock` takes `const CBlock&` only, and nothing binds the identifier list to the header. The first plan draft scoped 1.3.2 as a pure `CBlock` rule-reclassification and never named F-83 — that leaves the rung with nothing to run *over*, which is F-25e3's exact complaint ("the storage split, not the acceptance split"). Real scope: build `CheckCommitmentBlock` (the ~9-11 commitment-checkable rows from §2.4's table: PoW, header chain, merkle root over `Identifiers()`, **identifier uniqueness** and the coinbase leaf's own binding — not `id[0] == hash(coinbase)`, which F-78 already retired once 1.1 started carrying the coinbase whole; that comparison has no wire counterpart to check against — size, coinbase-is-first, coinbase `CheckTransaction` including the founder payment, coinbase finality, `bad-cb-type`) and an `AcceptCommitmentBlock`-shaped entry point that runs it **before** `nTx`/`nChainTx`/`HAVE_DATA` are ever recorded (F-83's ordering trap), and re-requests the list on failure rather than treating a bad list as a poisoned header. The 3 body-only rows with no connect-time home (F-44 — `nLockTime` finality, type/version, and `bad-txns-oversize`/F-44b, the one that matters most as §1A's own multiplier) relocate to connect time, **gated by the fork's activation height**: `ConnectBlock`'s own comment (`validation.cpp:2184-2194`) explains why it doesn't re-invoke `ContextualCheckBlock` — a newly-enforced rule can reject a block accepted by older software, and `-reindex-chainstate` already skips `ContextualCheckBlock` today. Relocating these 3 checks unconditionally would newly enforce them against **all of mainnet history** on every future `-reindex-chainstate`, which never checked them that way before; gating by height keeps pre-fork history's reindex behaviour exactly as it is today. **Verify with a full mainnet `-reindex-chainstate`** as this step's acceptance test — D-16's own reopen condition is "a rule turning out unrelocatable," and this is the test that would surface it. Separately, fix F-45's *present-tense* bug while in this code: `ProcessNewBlock` computes the founder-payment height as `Tip()->nHeight + 1` **before** taking `cs_main` — a guess — then caches the verdict in `fChecked`, and every later `CheckBlock` call (including `ConnectBlock`'s) short-circuits on it. This is live today, independent of decoupling; 1.1's `MaterialiseBlock` producing a fresh object doesn't touch it, since nothing in 1.3's scope materialises from fetched bodies yet (that's phase 2's job) — fix by clearing `fChecked` when the checked height disagrees with `pindex->nHeight`, or by caching the height alongside the flag. **F-45 fixed 2026-09-19 (F-102)**: moved the height computation inside `LOCK(cs_main)`, resolving the block's real parent via `LookupBlockIndex` instead of assuming it extends the tip — also fixes an unsynchronized read, not just the wrong-guess case. **`CheckCommitmentBlock`/`ContextualCheckCommitmentBlock` built 2026-09-19 (F-103)**: every commitment-checkable row from SS2.4's table, reusing `CheckBlockHeader`/`CheckTransaction` unchanged where possible, plus the new identifier-uniqueness rung rule (F-43b) and a needed `IsCoinBase()` guard on the wire-deserialized coinbase field neither F-83 nor the original plan named. Seven tests built from real mined blocks (not hand-constructed, to get DIP3/founder-payment details right by construction), mutation-tested. **The accept-pipeline wiring is not built**: neither function is called from anywhere yet — F-83's ordering trap (`nTx`/`nChainTx`/`HAVE_DATA` must not be recorded before these checks run; a bad list re-requested, not treated as a poisoned header) needs an `AcceptCommitmentBlock`-shaped entry point wired into wherever a raw `CCommitmentBlock` is received, which remains the real unbuilt half. **A second adversarial pass (F-104, 2026-09-19) found a real correctness bug before any wiring happened**: `CheckCommitmentBlock` ran its `IsCoinBase()` guard BEFORE the merkle-root check, with `corruption=false` — inverting `CheckBlock`'s own "corruption checks first" ordering, and exactly wrong for F-83's threat model (a genuine header paired with a wrong coinbase is corruption, not the header's fault; the old ordering would have let `AcceptBlock`-shaped wiring permanently ban a genuine header over it). Fixed, with a new test asserting `state.CorruptionPossible()` directly, since the existing reject-reason tests are (by design, to isolate individual checks) insensitive to ordering. Also fixed: `ContextualCheckCommitmentBlock` had dropped `ContextualCheckTransaction` on the coinbase, a real gap against the "every commitment-checkable row" claim (it also catches `bad-txns-type`, `bad-txns-cb-type`, and K-6's `bad-txns-oversize`, F-44's own connect-time-homeless rule). All fixes mutation-tested; full suite green. **1.3.2 finished 2026-09-19 (F-105).** `AcceptBlock` now runs `CheckCommitmentBlock`/`ContextualCheckCommitmentBlock` (derived from the full block via `CommitmentsFromBlock`, since nothing yet delivers a real standalone one) before `CheckBlock`/`ContextualCheckBlock` and before `nTx`/`nChainTx`/`HAVE_DATA` are recorded — F-83's ordering requirement, gated by `g_commitmentBudgetActive`. A new test proves the wiring is reached using the one rule `CheckBlock` genuinely lacks (`bad-cmt-duplicate-ids`): mutation-tested by disabling the call and confirming the identical bad block is instead fully *accepted*. `ConnectBlock`'s existing per-tx loop now also enforces `IsFinalTx` and `ContextualCheckTransaction` on every transaction, gated the same way — the F-44 relocation, verified via direct `ConnectBlock(fJustCheck=true)` calls since `ContextualCheckBlock` still shadows these checks on every real accept path today (documented, not a defect: they become load-bearing only once a future step makes `AcceptBlock` skip the full per-tx work for a genuine commitment-only arrival, which needs phase 2's body store). The mainnet `-reindex-chainstate` verification is satisfied by construction — the flag defaults false and nothing in production sets it, so both new sites are provably inert today, confirmed by the full 414-case suite passing unchanged. **Correction (F-115, 2026-09-20, found by a full-arc adversarial review after phase 1 was declared complete): "size" was listed above as a row `CheckCommitmentBlock` covers — it did not.** The function's own comment disposed of it together with sigops/byte-budget as "a bare identifier list cannot answer" — true for those two, wrong for identifier *count*, which a bare list's length answers directly. Fixed: rejects when `vCommitments.size()` exceeds D-19's `COMMITMENT_BUDGET_MAX_INPUTS` (reused, not a new constant — every non-coinbase tx needs >=1 input, so identifier count can never legally exceed the input-count budget). Two new tests, mutation-tested. | 4-5 w | **1.3.2 complete (F-115 closed the identifier-count gap)** — validation functions, accept-pipeline wiring and the connect-time relocation are all built, tested and reviewed |
| 1.3.3 | **`CheckBlockIndex` invariants and out-of-order arrival, on the real bit.** Most of this is 1.3.1's fatal-bug fix, already ported. What remains: reach F-25j's assert with a genuine out-of-order scenario. **Built and mutation-tested 2026-09-19 (F-108)**: a direct C++ test (`acceptancebit_tests.cpp`), not the mininode/P2P harness this row originally planned — `g_commitmentBudgetActive` is a test-only atomic with no wiring into a real running node binary at all (no arg, no RPC), so a functional test against the real binary could never exercise the commitment layer's interaction with out-of-order arrival regardless of Python availability; only an in-process boost test can flip it. `out_of_order_arrival_parks_a_child_until_its_parent_downloads` proves the guard still holds with `CheckCommitmentBlock`/`ContextualCheckCommitmentBlock` wired into the accept path. Building the fixture itself surfaced a real DIP3 constraint worth recording: a child block cannot be constructed on top of a parent that was never really connected (DIP3's CbTx merkle roots need the parent's deterministic MN list, which is only ever built incrementally) — confirmed by reproducing a fatal `EvoDB inconsistency` abort with the upstream `miner_tests.cpp` fake-tip trick. The working construction connects the parent for real, builds the child on the real tip, then rolls the parent back to header-only via the real `DisconnectTip` plus three invariants restored by hand (`setBlockIndexCandidates`, `nSequenceId`, the validity/data bits) — each found one at a time by `CheckBlockIndex`'s own assertions. **Fable adversarial review (2026-09-19) found this test didn't actually reach F-25j's gap**: it uses a fully-downloaded child under a header-only parent, exercising only `ReceivedBlockTransactions`'s generic parent-guard — unmodified Bitcoin Core code, still passes with `g_commitmentBudgetActive=false`, so "commitment layer wired in" was unproven. Kept (correct, useful, just not sufficient) and closed properly with a second test, **F-109**: `commitment_only_ancestor_parks_a_fully_bodied_descendant` builds a genuinely commitment-only PARENT (same `DisconnectTip`-based construction, downgraded to real commitment-only status rather than header-only) under a fully-bodied CHILD, and proves `FindMostWorkChain`'s `fMissingData` ancestor-walk (the actual 1.3.1-era, decoupling-specific mechanism) correctly refuses to connect it and parks it — directly asserting `m_blocks_unlinked` membership and candidate-set exclusion, not just relying on a crash. That mechanism is unconditional (not gated by `g_commitmentBudgetActive`), so this test correctly makes no claim about the resource-budget flag either. Full suite green (419 cases). | 1 w | **1.3.3 complete 2026-09-19 (F-109)** |
| 1.3.4 | **Equal-work tie-break — already decided by 1.3.1's design, prove it with a test.** `ReceivedBlockBodies` deliberately does **not** re-stamp `nSequenceId` (first-seen order hasn't changed when bodies merely catch up to already-held commitments) — this settles F-42's open question by construction, not as a separate decision to make. 1.3.4's job is a test proving a commitment-first, body-second arrival **does** win a tie against an equal-work rival that arrived whole in between — exactly what it would have won had it arrived whole first. (Corrected here: the original draft of this row had the direction backwards — F-35's bug was the block **losing** a tie it should have won, not winning one it shouldn't.) **Built and mutation-tested 2026-09-19 (F-107)**: `equal_work_tiebreak_survives_bodies_arriving_second` reproduces F-35's exact failure mode when `nSequenceId` is (mutated to be) re-stamped on body arrival — the tip wrongly stays on the rival. **Second correction (Fable review, 2026-09-19)**: this is NOT "matching unmodified Bitcoin's tie-break", as first written here and in F-107 — it is the opposite side of F-42's own open question. Unmodified Bitcoin only assigns `nSequenceId` when a block's full data arrives, so a commitment-first/body-second block can never win a tie there; here it can. F-42 records this as a disclosed, not-yet-Mike-approved trade-off (a miner can reserve a tie by delaying a body), not a preserved behaviour. | 2-3 d | **1.3.4 complete** |
| 1.3.5 | **`VerifyDB` / crash replay / `-reindex`.** `CValidationState::BodiesMissing()` (`validation.cpp:3139`, one setter, one reader at `init.cpp:1193`) already turns "corrupted, please -reindex" into an honest "bodies missing, not corrupt" (F-25/F-25c); `VerifyDB` already stops at a `HaveBodies()`-shaped check (`validation.cpp:5053-5069` on the probe branch) — re-point both at 1.3.1's real bit once rebased, and extend to `-reindex`/`-reindex-chainstate` (interacting with 1.3.2's height-gated relocation above). **Done 2026-09-20 (F-110), scope corrected by a Fable review before any code was written**: the premise was wrong about which mechanism is load-bearing — `ConnectTip`'s guard and `init.cpp:1193` are both dead code today, since `FindMostWorkChain`'s own ancestor-body-gap filter (F-109's mechanism, added on the probe branch 60 minutes after F-25c's own commit, before this session existed) already excludes a bodies-missing candidate before `ConnectTip` ever runs. Both guards are kept as defence in depth. `VerifyDB`'s guard is real and already correctly wired to the rebased bit; it just had zero test coverage, and needs `g_perf_withhold_hashes` (not bit-surgery alone) to genuinely exercise it, since a block's bytes are always fully written to disk under today's storage format regardless of the bit. `LoadExternalBlockFile`'s M-2 fix (F-104) also had zero coverage — and, worth recording (corrected: this is `-loadblock`'s behaviour, not `-reindex`'s, which always starts from a wiped index and so never has a commitment-only entry to encounter), `-loadblock` currently *heals* a commitment-only entry back to fully-bodied rather than merely tolerating it, since every blk-file offering is inherently complete pre-Phase-2. `ReplayBlocks` needed no fix (its recovery targets can only ever be blocks that already passed `ConnectTip`'s bodies-present path). Three new tests, all mutation-tested, all passed without any production-code change. Full suite green (422 cases). | 3-5 d | **1.3.5 complete 2026-09-20 (F-110)** — turned out to be nearly all test coverage, not new logic |
| 1.3.6 | **Retry backoff, derived at load — not a second persisted queue.** `m_blocks_unlinked` already re-arms retry for free (F-25f) and is itself derivable at load from the index (any `HAVE_DATA ∧ ¬HAVE_BODIES` block); a **second** durable structure recording the same fact can disagree with the index after a crash. Keep backoff **memory-only**, keyed per missing body independent of that block's chain-work standing (today's work-keyed heuristics are exactly wrong for a gap that isn't on the current best-work chain), rebuilt from the index on every start — and, §2.3's inviolable rule, never marks the block invalid regardless of how long the wait runs. Note the interaction with `mapBlocksInFlight`/`MarkBlockAsInFlight` and the staller logic when two requesters want the same missing body. **Done 2026-09-20 (F-111), scope corrected by a Fable review before any code was written**: the "new backoff structure" premise was itself off-target — the real gap was a single unfixed line, and existing peer-timeout-and-reassignment machinery already provides the backoff this row asks for (no NOTFOUND for blocks; a non-delivering peer is eventually disconnected, the block never marked invalid, and `FindNextBlocksToDownload` naturally reassigns it). The actual bug: that function's own cursor (`pindexLastCommonBlock`) advanced past a body-missing block whenever a fully-bodied descendant existed, because such a descendant's `HaveTxsDownloaded()` reads true (commitments alone count as "downloaded", decoupling's whole point) — F-25e2 exactly, which the 1.3.1 row above wrongly claimed was already fixed; it was not. Fixed with a one-call-scoped `fBehindGap` flag. Proven live (not just by restart) with a new test-only `-perfwithholdcount=<n>` flag and a real-P2P functional test (`feature_body_refetch.py`) — building that test surfaced two further real bugs (a shared withhold-attempt counter read from three call sites, burning through the configured count inconsistently; and a fatal `AbortNode` once the count exhausted, since a separate, unrelated withhold check had no way to know) — see F-111 for both. All three fixes mutation-tested against the same live test. **Correction (F-114, 2026-09-20): the "existing machinery already provides the backoff" claim above was wrong.** That machinery detects a peer going silent; it does nothing when a peer keeps answering but the answer never resolves the gap (`-perfwithholdcount`'s own shape, forever-withheld) — `MarkBlockAsReceived` frees the in-flight slot on every response regardless of outcome, so neither timeout ever engages. Measured directly: ~1,100-3,600 `getdata` requests/second for one block, unbounded. A full-arc adversarial review (two independent models, run after phase 1 was declared complete) found this; a second reviewer independently reproduced the measurement before it was accepted. **Actually fixed 2026-09-20 (F-114)**: `g_body_retry_state` (`net_processing.cpp`) — the memory-only, self-cleaning, exponential-backoff map this row asked for in the first place, consulted in `FindNextBlocksToDownload` before re-adding a still-missing block to the fetch list. New functional test `feature_body_refetch_backoff.py`, mutation-tested. | 1 w | **1.3.6 complete 2026-09-20 (F-114) — F-111's backoff claim was wrong, now actually built** |
| 1.3.7 | **`InvalidateBlock` / `MarkConflictingBlock` / `EnforceBestChainLock` — audit, not full hardening.** Confirm these three don't crash or silently corrupt state when invoked against a commitment-only block — including `ReadBlockFromDisk`'s `nullptr` fallback on such a block reaching an IS-conflict-detection caller safely. Full graceful degradation (`EnforceBestChainLock`'s `assert(false)` made non-fatal, etc.) is 4.1's job — 1.3 only needs to prove these three are safe under the new state, not polished. **Done 2026-09-20 (F-112), scoped by a Fable review before any code was written**: both functions' shared candidate-rebuild loop can reinstate a commitment-only block as a candidate (an existing, not new, gap already relied on elsewhere), but `FindMostWorkChain` (F-109) is the one already-tested filter and nothing else reads the set unsafely. `EnforceBestChainLock`'s backward walk and its one `assert(false)` path are safe by construction (a commitment-only block can never be `m_chain.Contains()`, so `DisconnectTip` is never asked to disconnect one). The row's own named `ReadBlockFromDisk`/IS-conflict-detection deliverable — missed by the first scoping pass, found by the review — turned out to be `TrySignChainTip`'s `GetBlockTxs` walk, rooted at the active tip and never reaching a commitment-only block either. Four new tests (`acceptancebit_tests.cpp`), mutation-tested, reusing the lighter equal-work-siblings construction (F-107) since neither audited function touches DIP3/evoDB state directly. Full suite green. | 3-5 d | **1.3.7 complete 2026-09-20 (F-112) — phase 1 complete** |
| 1.4 | **Fork envelope** (D-14). The 8 MB cap (K-1) and the message-length raise (K-7) ride this activation; 2 MB by policy. | in 4.6 | straightforward |

**What cold connect costs, and why the cache is load-bearing** (measured 0.3). A full commitment
block of *ordinary* two-in/two-out payments, when the bodies were never seen at acceptance:
~16-20 s single-thread at 2 MB and ~65-78 s at 8 MB (F-16); ~7 s and ~28 s on a 4-thread box; ~4-5 s and
~17-20 s on the Smartnode spec of X-5. **Warm — validated at acceptance — the same block is
~0.8 s** (F-15b, F-16). A 20-25× swing, which makes three things follow: 2.3's relay cap re-index is what keeps
the cache hitting, not just what makes reconstruction possible; `-maxsigcachesize` in 4.5 is
consensus-adjacent sizing rather than tuning, since K-4's 524,288 entries is only 2.1 blocks at 8 MB;
and the cold column is a third independent argument for mining 2 MB by policy (D-14), beside relay and
storage.

**Why 1.2 is not optional** (K-2, F-13). `MaxBlockSigOps() = MaxBlockSize()/50` is 40,000 at 2 MB, and
`GetLegacySigOpCount` counts sigops per output (F-13), so a two-output payment is 2. A 2 MB commitment
block naming 62,500 payments carries 125,000 sigops and is **invalid** (K-2) — the cap admits about
20,000, i.e. **167 tx/s, not 520** (K-2), at the 2 MB consensus cap (F-46b: this branch's rig-only
8 MB build gives 667 tx/s instead — re-derive against whichever cap D-14's activation ships) — and
the miner silently stops filling. It is the relay cap bug in a second place: a consensus limit
indexed to block bytes, which stop tracking what the block commits to. **Re-basing it is now scoped
but not built** — see R-32, D-17 and §1A's revision in `transaction-decoupling.md`.

## Phase 2 — data plane · 7-11 weeks

| # | Component | Effort | Design |
|---|---|---|---|
| 2.1 | **Body store.** Third flat-file series in block order beside `blk*.dat` and `rev*.dat`, with a per-block offset table — block order because assembly is the hot path. Written during `AcceptBlock` transactionally with the commitment write, **never** at mempool acceptance. Self-delimiting for `-reindex`, delete-capable, and it drops losing siblings' bodies once buried. **2.1.1 built 2026-09-20 (F-126), reviewed and hardened same day (F-127).** `bodystore.h`/`.cpp` — a fourth `FlatFileSeq` (`bdy*.dat`), independent file numbering from `BlockFileSeq()`/`UndoFileSeq()` (decided, not left open: a commitment block's bytes are small and roughly constant, its bodies range from empty to the full resource-budget ceiling, so coupling rotation to blk/rev's would be a many-to-one relationship with no benefit). Per-block record is self-delimiting: `[CompactSize count][count x fixed 4-byte cumulative end-offset][tx bytes...]` — **corrected (F-127): the offsets were originally `CompactSize` too, which refuses anything over 32 MiB, well under the 110 MB body budget, making a full-size record unreadable; fixed to fixed-width, with a `static_assert` tying the format to the real budget.** `ReadBodyAt` seeks directly to any index via a real `fseek` — **corrected (F-127): the first version used `CAutoFile::ignore()`, which still reads every skipped byte, so "O(1)"/"seeks directly" was not true until this was fixed.** `FindBodyPos`'s rollover matches `FindBlockPos`'s condition (`MAX_BODYFILE_SIZE` = 128 MiB, matching `MAX_BLOCKFILE_SIZE`) and now also finalizes (truncates + fsyncs) the file it leaves on rollover, matching `FindBlockPos`'s own `FlushBlockFile` call — **corrected (F-127): this was missing at first ("mirrors FindBlockPos exactly" was not true); the gap was concretely evidenced by leaked, never-truncated `bdy*.dat` files this same review found sitting in a real datadir.** All 2.1.1 fixes mutation-tested (4 mutations, all caught). **2.1.2 built 2026-09-20 (F-128): the file bookkeeping now persists.** `CBodyFileInfo` (`bodystore.h`) is `CBlockFileInfo`'s persisted analogue -- simpler, just `nSize`, since nothing here is a pruning/reindex decision yet. New `CBlockTreeDB` methods (`txdb.h`/`.cpp`, new DB key prefixes `y`/`Y`) mirror `ReadBlockFileInfo`/`ReadLastBlockFile`/`WriteBatchSync`'s own shape but write a separate batch, independent of the block-index flush. **Correction (2.1.2/2.1.3 review): "has nothing to do with the block-index flush's atomicity" stopped being true the moment 2.1.3 added `nBodyFile`/`nBodyPos` to `CDiskBlockIndex`** -- an index entry can now name a body position before the body-file bookkeeping that makes that position safe from reuse is itself durable. See `WriteBodyFileInfoBatch`'s own corrected doc (`txdb.h`) for the exact crash scenario and the ordering 2.1.4 must enforce (or the alternative of folding this into `WriteBatchSync` itself) before wiring any real write. `LoadBodyFileInfo`/`FlushBodyFileInfo` (`bodystore.cpp`) load from and write dirty entries back to `pblocktree`, tracked via `setDirtyBodyFileInfo` marked inside `FindBodyPos`. Proven with a real restart simulation (`TestOnlyResetBodyFileState`, test-only): write, flush, wipe in-memory state, reload, confirm `FindBodyPos` continues from the persisted position rather than reusing file 0 and silently overwriting the record just written -- both mutations tried (skip the reload; skip marking dirty) reproduced exactly that overwrite before being caught. **Callable and correct, not yet wired into real node startup/shutdown** -- there is still no `LoadBodyFileInfo()` call in `init.cpp`/`LoadBlockIndexDB` or `FlushBodyFileInfo()` call in `FlushStateToDisk`, deliberately deferred to land alongside 2.1.4, since nothing in production calls into `bodystore.cpp` at all yet. **2.1.3 built 2026-09-20 (F-129): `CBlockIndex` can now record a body-store position, needing no migration.** New `BLOCK_HAVE_BODY_RECORD` status bit plus `nBodyFile`/`nBodyPos`/`GetBodyPos()`, on `nFile`/`nDataPos`/`GetBlockPos()`'s own pattern -- `CDiskBlockIndex` already conditionally serializes those three fields based on `nStatus` bits, confirmed by reading it before designing anything, so the new fields extend an established, already-safe pattern rather than inventing one. **Deliberately its own bit, not a reuse of `BLOCK_HAVE_BODIES`**: that bit is true for essentially all of history under Phase 1's storage format (F-110), so gating the new fields on it would require every existing on-disk entry to already have them, and none do. A bit false for every entry until 2.1.4 sets one for real needs no active migration, unlike `BLOCK_HAVE_BODIES`'s own 1.3.1 one (F-101) -- proven with 3 tests, not just argued: `GetBodyPos()` null until the bit is set, a full round-trip with every other field checked unaffected, and two otherwise-identical entries (one with garbage in the new fields but the bit unset) serializing byte-identical -- mutation-tested by forcing unconditional serialization, caught immediately. A downgrade hazard once 2.1.4 genuinely sets the bit is real but general to any format-extending change and inert today; recorded against component H's dual-format period, not solved here. **`-reindex` decided (owner, 2026-09-20): match `blk*.dat`/`rev*.dat`'s existing behaviour exactly, no new mechanism.** Confirmed directly against `init.cpp` before proposing this: `-reindex` already wipes `pblocktree` (`fReset = fReindex`, passed as `CBlockTreeDB`'s `fWipe`) and never deletes `blk*.dat`/`rev*.dat` themselves outside prune mode (`CleanupBlockRevFiles()` is prune-only); `ThreadImport`'s `-reindex` branch then re-scans every `blkNNNNN.dat` in order and re-processes every block through the normal `LoadExternalBlockFile` -> `ProcessNewBlock` -> `AcceptBlock`/`ConnectBlock` path, exactly as if freshly received -- the whole index and file bookkeeping (`nFile`/`nDataPos`/`nUndoPos`, `vinfoBlockFile`, `nLastBlockFile`) is rebuilt from file 0 forward as a **side effect** of reprocessing, overwriting old bytes with equivalent new ones. This is wipe-the-index-and-rederive-by-reprocessing, not scan-and-reconcile -- `blk*.dat` records were never self-identifying either. Applied to the body store: `pblocktree`'s existing wipe already covers `CBodyFileInfo`/`nLastBodyFile` and `CDiskBlockIndex`'s `nBodyFile`/`nBodyPos` (same database); `bdy*.dat` files are never deleted, matching `blk*.dat`/`rev*.dat`; **as long as 2.1.4's `AcceptBlock` wiring writes a body record unconditionally on every accept** (mirroring `SaveBlockToDisk`'s own unconditional write, reindex or not), reindex naturally rebuilds `bdy*.dat` from file 0 forward for free. **No self-identifying record header needed** -- that would have been a more robust but *different* model than what `blk*.dat` already does; matching "now" means the simpler one. No new code for `-reindex` specifically; 2.1.4 just must not special-case "is this a reindex" in its write path. **Flush ordering decided and implemented (owner, 2026-09-20, F-132): fold into `WriteBatchSync`.** `CBlockTreeDB::WriteBatchSync` (`txdb.h`/`.cpp`) gained trailing, defaulted `bodyFileInfo`/`nLastBodyFile` parameters (sentinel `-1` means "nothing to report", used by the one untouched call site, `LoadBlockIndexDB`'s pre-1.3 migration) and now writes `DB_BODY_FILES`/`DB_LAST_BODY_FILE` into the SAME atomic batch as block-file-info/last-file/block-index -- no separate, independently-timed write left to reorder incorrectly. The old `WriteBodyFileInfoBatch` is gone; `bodystore.cpp`'s new `GetDirtyBodyFileInfo` is a getter (gather + clear the dirty set), not a flush, and `FlushStateToDisk`'s real call site (`validation.cpp`) now calls it and threads the result through on every real flush -- genuinely live wiring, not dead code, though still inert today since nothing populates the dirty set until 2.1.4's `AcceptBlock` wiring lands. Mutation-tested (3/3 caught, including a migration-safety regression test proving the sentinel actually protects the untouched call site). **Reviewed same day (F-133)**: found and fixed a mutation-provable test blind spot (nothing drove `WriteBatchSync` with `nLastBodyFile == 0`, so a `>` vs `>=` bug in the guard would have silently dropped ALL body-file persistence for a node's entire first ~128 MiB -- new boundary test closes it, independently confirmed against the reviewer's own mutant) and a latent pointer-lifetime hazard (`GetDirtyBodyFileInfo` returned raw pointers into `vinfoBodyFile` after releasing its own lock, unlike the block-file family, which stays protected by `cs_LastBlockFile` held across the whole flush -- fixed by returning `CBodyFileInfo` by value instead, removing the hazard class rather than documenting an invariant). Also found and RECORDED two real gaps that are 2.1.4's job, not fixable in isolation: (1) no body-file equivalent of `FlushBlockFile()` exists yet -- nothing flushes the CURRENT body file's data outside of rollover-time finalize, so a crash right after this batch goes durable can leave an index entry naming bytes that never left the page cache; 2.1.4 must add that call (guarded against `vinfoBodyFile` being empty pre-load) alongside `FlushBlockFile()`'s own call site. (2) the write side is now live on every flush but the load side (`LoadBodyFileInfo`) still isn't wired to startup -- 2.1.4 MUST land both in the SAME change, never the write first, or the first flush after that change persists a fabricated `nLastBodyFile=0` over a real value, reproducing F-130's overwrite hazard by a new route. **2.1.4 built 2026-09-21 (F-134), reviewed same day (F-135) -- Phase 2.1 complete, review findings fixed.** F-135 found two real HIGH bugs in F-134's own restart claim: (1) `LoadBlockIndexGuts` (`txdb.cpp`) never copied `nBodyFile`/`nBodyPos` back from `CDiskBlockIndex` on reload -- a correctly persisted position was silently dropped on every restart; fixed with the two-line copy, mirroring the existing fields' own style. (2) `BLOCK_HAVE_BODY_RECORD` was wrongly modeled as `BLOCK_HAVE_BODIES`'s sibling (both deferred under withholding), but `CDiskBlockIndex` only serializes the position when that bit is set -- so a withheld block's real position was never durable at all. Root cause: the bit's actual job is `BLOCK_HAVE_DATA`'s job for the body store ("we know where these bytes are"), not `BLOCK_HAVE_BODIES`'s; fixed by making it unconditional, exactly as unconditional as `BLOCK_HAVE_DATA`. Both fixes mutation-tested (reverted, confirmed the real mutant fails, re-fixed). Also fixed: `SaveBodyToDisk` now runs BEFORE `SaveBlockToDisk` at both call sites (a body-side failure no longer orphans real block bytes in `blk*.dat` forever); `UnloadBlockIndex` now resets body-store state too (a new non-test-only `ResetBodyFileState()`, promoted from the test-only helper). Two test-quality gaps closed (a connectivity check, a file-1 roll to catch a wrong-file mutant). One reviewer suggestion (a `CheckBlockIndex` invariant) evaluated and declined -- it would trip two existing pruning-simulation tests, since pruning doesn't clear `BLOCK_HAVE_BODY_RECORD` yet (F-130's own still-open item, not this review's to solve). Full suite green (455 cases via `!miner_tests`). `SaveBodyToDisk` (`validation.cpp`) mirrors `SaveBlockToDisk`'s shape and is called unconditionally alongside it at both real call sites (`AcceptBlock`, `AddGenesisBlock`), regardless of `-perfwithholdbodies` or `-reindex`, per F-131's decision. `ReceivedBlockTransactions` sets `nBodyFile`/`nBodyPos` unconditionally and `BLOCK_HAVE_BODY_RECORD` only when `bodies_held`, exactly the existing `BLOCK_HAVE_BODIES` split; `ReceivedBlockBodies` sets the same bit when bodies "arrive" later, reusing the position already on disk -- no second write. Both F-133 constraints landed together as required: `LoadBodyFileInfo()` in `LoadBlockIndexDB` right after the block-file-info load, and `FlushBodyFile()` (FindBodyPos's own `FlushBlockFile` analogue, guarded against an empty `vinfoBodyFile`) in `FlushStateToDisk` right after `FlushBlockFile()`. Six new tests (two in `bodystore_tests.cpp`, four in `acceptancebit_tests.cpp` exercising the real `AcceptBlock`/`ProcessNewBlock`/`LoadBlockIndexDB` path, including a withhold-then-arrive-later test mirroring the existing `BLOCK_HAVE_BODIES` one and a real-reload test proving the write and load sides actually agree after a restart). Building this found and fixed two real test-design traps (a reload assertion that let both sides of a comparison collapse to the same wrong zero under a real mutant; `FlushStateToDisk` itself is unsafe to call directly from this test fixture, unrelated to the body store) and one real test-fixture bug (`TestOnlyResetBodyFileState` used `vector::clear()`, which keeps capacity, so a "reset" vector could still read stale data from an earlier test case instead of faulting -- fixed to a true zero-capacity reset). 6 mutations tested, all caught. Full suite green (454 cases via `!miner_tests`), same one pre-existing `miner_tests` failure. **Deliberately still out of scope**: nothing reads from the body store in production -- this is a parallel, redundant write path alongside `blk*.dat`, populating the store correctly so a later phase can cut a real read path over to it. Still open, not yet decided, and not blocking anything: how pruning a body record interacts with the offset table (the "can delete later" requirement, §8.4) once anything actually prunes; whether `BLOCK_HAVE_BODY_RECORD` should imply `BLOCK_HAVE_BODIES` and if so what that means for `PruneOneBlockFile`, which doesn't clear the new bit today (harmless while nothing sets it) | 2-3 w | **Phase 2.1 (the body store) complete: 2.1.1-2.1.4 built and reviewed (4 review rounds: F-127, F-130, F-133, F-135)** |
| 2.2 | **Fetch protocol.** `(blockhash, index/range)` addressing (F-143: the height-addressing form `(height, index/range)` originally proposed here was dropped during 2.2.2's own spec review -- every requester already holds the hash it wants, and 2.2.1's index resolves hash-form lock-free regardless of chain position). Served off `cs_main` on the block path from the store's own hash index; NOTFOUND ≠ timeout; works during initial block download; range-batched; responses chunked; per-connection budgets folded into upload accounting; multi-source. **Spec reviewed and accepted 2026-09-21 (F-139), not yet built.** Phased as 2.2.1 (a new body-store-owned index -- `blockhash -> pos` populated at record-write time since side-chain blocks never connect; `height -> pos` connect/disconnect-maintained, active chain only; a dedicated, leaf-most lock never shared with `cs_LastBodyFile` and never held across I/O), 2.2.2 (wire format -- **superseded by its own spec review, F-143, see below**: `SENDCOMMITMENTS`'s own precedent for the message types, reusing `NODE_COMMITMENTS` as the capability bit rather than a new one; NOTFOUND non-punitive for history, misbehaviour from a block's own announcer within a retention-coupled tip-path window per §14.1 rule 2/§8.4 decision 2/§14.8a -- the announcer check itself is a new per-peer mechanism, not the addressing form, since only hash-form requests exist now; every response echoes the block hash it was served under so an honest reorg race reads as "retry", not misbehaviour; chunked to fit `MAX_PROTOCOL_MESSAGE_LENGTH` via a byte ceiling, not a count cap), 2.2.3 (serving -- **genuinely new code, not a port**: `net_processing.cpp`'s only existing block-serving path, `ProcessGetBlockData`, was found to hold `cs_main` across its own disk read despite this doc's own §13.F previously claiming otherwise -- corrected in place; the new handler resolves through 2.2.1's index and never touches `cs_main` at all), 2.2.4 (fetching -- single-source for this build, multi-source deferred to 2.4; the previously-unbuilt body-ARRIVAL write path, `MaterialiseBlock` → the body-dependent `CheckBlock`/`ContextualCheckBlock` rows → `WriteBodyRecord` → status bits → both indexes, under F-133/F-135's existing crash-ordering discipline; B3's real remaining gap is an aggregate concurrent-chase cap plus re-keying the existing `g_body_retry_state` backoff to body-ranges, not new backoff logic; C1's source-preference scoring). The old 2.2.5 (relay/ChainLock hardening) was dropped as its own phase -- it substantially duplicated 4.1, which already scopes it with its own estimate and gate on 4.6; 4.1 should start as an audit, since Rule 1 is largely already satisfied by construction (F-117). Rate-limit budgets, chunk size and the concurrent-chase cap ship config-driven with conservative defaults, measured later via the same `loopback`/swarm methodology as F-4/F-5, not guessed now. **2.2.1 built 2026-09-21 (F-140), reviewed and fixed 2026-09-21 (F-141)** -- the body-store-owned height+hash index itself: `RecordBodyPositionByHash`/`LookupBodyPositionByHash`/`RecordBodyPositionAtHeight`/`EraseBodyPositionAtHeight`/`LookupBodyPositionAtHeight`/`ResetBodyIndex` in `bodystore.h`/`.cpp`, wired into `ReceivedBlockTransactions` (hash, record-write time), `ConnectTip`/`DisconnectTip` (height, active chain only, overwrite/erase-outright), `LoadBlockIndexDB` (hash-half rebuild) and `CChainState::LoadChainTip` (height-half rebuild) -- the two rebuild halves deliberately split across two functions per F-141 (below). `bodystore.h`/`.cpp` still knows nothing about `CBlockIndex`/`CChain`/`BlockMap`. **F-141's Fable review** found the index itself correct (lock discipline, reorg-by-hand walk both independently verified) but its boot/rebuild wiring genuinely broken on two real paths: a `-reindex-chainstate` boot (a distinct flag from `-reindex`, confirmed against `init.cpp`) skipped `LoadChainTip` entirely, leaving the hash half permanently empty for the process's life -- fixed by moving the hash-half rebuild into `LoadBlockIndexDB`, which runs on every load path except a full `-reindex` (where `ReceivedBlockTransactions` repopulates it naturally); `UnloadBlockIndex` never reset this index (only `ResetBodyFileState()`), so a reindex retry AND every test fixture's own teardown inherited stale entries -- fixed by adding `ResetBodyIndex()` there. Also fixed: `ConnectTip` recorded a null position unguarded for a genuinely pre-2.1.4-style entry reachable via `-reindex-chainstate` (`chain.h`'s own migration note confirms such entries exist), poisoning the height map with `nFile == -1`; `DisconnectTip` erased its height entry after `m_chain.SetTip` rather than before, leaving a brief window where a `cs_main`-free reader could see stale data. 4 new regression tests confirmed to fail against the pre-fix code (a 5th, the loop-source-swap coverage test, does not fail pre-fix by design -- it targets a mutant, not this commit's own bug), plus the existing `LoadChainTip` reload test updated to also call `LoadBlockIndexDB`. **A second Fable review of F-141's own fix (F-142)** confirmed all five fixes correct by re-derivation from source (including tracing every real `SetTip` call site to rule out a stale-entry survival path for the `ConnectTip` guard), but found the loop-source-swap coverage test's original equal-work-sibling construction was probabilistic, not deterministic: `BlockMap`'s hasher is the block's own unsalted PoW hash, so which of two same-height competitors an `unordered_map` visits last is effectively a coin flip (measured ~60% mutant-survival across 100k simulated runs) -- fixed by replacing the competing sibling with a single withheld (`PerfWithholdGuard`) child that can never connect at all, so under the mutant ANY write to its height is wrong regardless of iteration order (re-verified: 8/8 deterministic kills). Two stale doc comments (still naming `LoadChainTip` as the reset/rebuild site) corrected in `bodystore.h`/`bodystore_tests.cpp`. Left as an explicit open decision for 2.2.2/2.2.3, not a defect: the index answers "where are the bytes", not "may I serve them" -- a withheld (`-perfwithholdbodies`) block is hash-resolvable but not `HaveBodies()`-servable, so 2.2.2's NOTFOUND semantics must decide whether the index carries its own serveability flag or tolerates "found in index, refused at serve". **Also noted (LOW, informational, not a defect)**: a pre-2.1.4-synced datadir only ever gets real body-store records via a full `-reindex`, never `-reindex-chainstate` (which only reconnects existing entries, never re-runs `ReceivedBlockTransactions`) -- an operator upgrading an old datadir who wants 2.2.3's fetch/serve path to work over their existing history needs `-reindex`, not `-reindex-chainstate`; worth surfacing in whatever upgrade notes eventually accompany 2.2's deployment. TDD (9 `bodystore_tests.cpp` cases, 8 `acceptancebit_tests.cpp` integration cases total after F-141's additions), mutants killed for every fix with a deterministic reproduction (the one LOW-severity ordering fix has none -- a sub-instruction race window, fixed by reasoning, its own revert-mutant survives the full suite). Full suite green (492 cases, `!miner_tests`). **2.2.2's wire-format spec written and reviewed 2026-09-22, accepted (F-143), not yet built.** Five review rounds (four full, one narrowed section repeated twice) rebuilt two pieces from the ground up: **addressing is now hash-only** -- `CGetBodyRange{hashBlock, nStartIndex, nCount}`/`CBodyRange{hashBlock, nStartIndex, vBodies}`, no `fFound` bool (miss is `vBodies.empty()`) -- the original `(height, index/range)` form from this row's own opening line is DROPPED (owner decision, re-opening that part of F-139): every requester already holds the hash it wants, 2.2.1's index resolves hash-form lock-free regardless, and the height form was the specific, repeated cause of two of the five HIGH findings in round 2. **NOTFOUND classification is now a height-bounded, per-peer "announcer ring"** (populated at three `net_processing.cpp` call sites via one shared helper, depth coupled to -- never exceeding -- this project's still-unfixed minimum body-retention floor), replacing two successively-broken designs (a global "last announcer wins" map, then reusing the existing but wrong-purpose `pindexBestKnownBlock`/`PeerHasHeader`) -- explicitly does NOT close a withholder simply going silent rather than answering (§14.8a's own accepted shape of the problem, recorded not fixed). Chunking is a byte ceiling from the body record's own offset table (not count-based -- a count cap sized for the worst case only fits 3 bodies under `MAX_PROTOCOL_MESSAGE_LENGTH`), with a real server-side budget (backpressure via the existing send-buffer mechanism, a per-connection records-and-bytes window, a per-file-opened charge, folded into `-maxuploadtarget`) replacing what would otherwise have been a client-side-only self-limit that defended nothing against B4. **Retroactively amends 2.2.1's own index API, built 2026-09-22 (F-144)**: `RecordBodyPositionByHash`/`LookupBodyPositionByHash` gain a serveability flag alongside position (withheld bodies read as an indistinguishable miss on the wire, closing F-141/F-142's own open item) -- required to write (no silent default), optional to read (nullable out-pointer, so ~15 pre-existing position-only call sites needed no change). Wired at all three real `BLOCK_HAVE_BODIES` write sites (`ReceivedBlockTransactions`, a new call in `ReceivedBlockBodies`, and F-141's own rebuild loop in `LoadBlockIndexDB`). TDD (4 `bodystore_tests.cpp` + 2 `acceptancebit_tests.cpp` cases, one a real withhold-then-arrival cycle end to end), 5/5 mutants killed. One real test-authoring bug found and fixed (not production): a rebuild test's own withheld block collided with the pre-1.3 `bodiesmigrated` migration's first-run heuristic, and a naive fix (calling `LoadBlockIndexDB` early) itself tripped a `CheckBlockIndex` invariant by reprocessing live fixture state mid-test -- fixed by setting the migration flag directly instead. **A flakiness surfaced and was chased -- RETRACTED as a real finding, root-caused as a self-inflicted stale-build artifact.** Repeated full runs showed 0/6 failures on a clean build of the base commit vs. ~2/10 on an incrementally-rebuilt binary carrying this change (a `SIGABRT` and an apparent deadlock at `~CCheckQueue()`-adjacent teardown in unrelated, untouched tests). First hypothesis (pre-existing concurrency issue in the script-check worker-thread lifecycle) was disproved directly: that binary had been incrementally rebuilt dozens of times across this same session's own rapid mutation-testing edit/rebuild/restore cycles; a `make clean` plus one full rebuild, then 8 further full runs, produced 8/8 clean. Almost certainly a stale-`.o`/dependency-tracking miss from that much rapid churn, not a codebase defect -- see F-144's own docs/findings.md entry for the full account. **Reviewed 2026-09-22 (F-145)**: 1 HIGH (the spec's OWN clear site, `PruneOneBlockFile`, was never wired -- a pruned node's index could answer "serveable" for a block `HaveBodies()` says is gone, until a restart happened to rebuild it correctly; fixed, re-records `fServeable=false` on prune), 2 MEDIUM (a mutant the author's own mutation pass never tried -- `ReceivedBlockTransactions` always recording `false` -- survived the whole 67-case surface that reads the flag, since no test ever checked a normal block's value immediately after accept; fixed with a control-block assertion, a position-stability check, and a direct `HaveBodies()` cross-check. Also: the optional nullable read API made the UNSAFE call the ergonomic default; fixed by adding `LookupServeableBodyPositionByHash`, false unless both present and serveable, so 2.2.3 has a plain-signature safe call to build on), 2 LOW (a missing `IsNull()` guard in `ReceivedBlockBodies` for a pre-2.1.4-style entry, matching F-141's own already-fixed `ConnectTip` bug; a rebuild-loop comment overclaiming the migration's bit would reach the index if written there, when in practice it never can). Full suite: 503 cases (`!miner_tests`) clean on a full clean rebuild, up from 498. **2.2.2's own wire structs, message types, and request-validation classification built 2026-09-22 (F-146)** -- new `bodyrange.h`/`.cpp` (matching `evo/simplifiedmns.h`'s own per-domain request/response-struct convention, not `protocol.h` itself): `CGetBodyRange{hashBlock, nStartIndex, nCount}`/`CBodyRange{hashBlock, nStartIndex, vBodies}` (hash-form only, no `fFound`), `VtxIndexFromBodyIndex`/`BodyIndexFromVtxIndex` (named per F-115's own precedent for this off-by-one class), and `ValidateGetBodyRange` implementing F-143's exact three-tier BAN/MISS/OK classification (no `cs_main` anywhere, via F-145's `LookupServeableBodyPositionByHash`). `NetMsgType::GETBODYRANGE`/`BODYRANGE` registered in `protocol.h`/`.cpp`. Deliberately NOT delivered, per spec: any `ProcessMessage` dispatch arm, config knobs, truncation/continuation logic, announcer-ring wiring -- all 2.2.3/2.2.4. TDD (12 `bodyrange_tests.cpp` + 1 `acceptancebit_tests.cpp` case pinning the index-space invariant against a real two-tx accepted block), 5/5 mutants killed. Verified via a genuine clean rebuild (this session's own F-144/F-145 lesson about incremental-build staleness during rapid mutation testing). Full suite: 516 cases green (`!miner_tests`, up from 503). **Reviewed 2026-09-22 (F-147)**: 2 HIGH sharing one root cause (`bodyrange_tests.cpp` copied `BasicTestingSetup` directly rather than F-127's own established scratch-datadir fixture pattern, so its real `FindBodyPos`/`WriteBodyRecord` calls wrote into a live personal node's actual `~/.raptoreumcore` datadir -- confirmed via `ps`/`stat`, not assumed; fixed with a `BodyRangeTestingSetup` matching `bodystore_tests.cpp`'s own, verified via an unchanged file mtime across a full suite run, stray file left for the owner to remove per F-127's own precedent -- and the SAME contamination had made F-146's "ignore the lookup's return value" mutant falsely appear to die via coincidental garbage bytes at a never-written test position; fixed by backing that test with a real written record). Re-verifying that mutant on the corrected fixture found it now survives for a genuinely benign, structural reason rather than a hidden gap: `LookupServeableBodyPositionByHash`'s own MEDIUM fix (below) means a caller ignoring its return value still gets an untouched, default (`nFile=-1`) position, which `ReadBodyRecordCount` fails to open regardless of the underlying hash's true state -- the real serveability property is instead caught, 4/4, by mutating `LookupServeableBodyPositionByHash` itself. 1 MEDIUM (the same function leaked a withheld block's real position into the caller's `posOut` on its false-return path; fixed by resolving into a local position first). 3 LOW (a round-trip test never asserted two of its own fields; a missing bounds check let an off-by-one mutant crash via UB instead of failing cleanly; two missing-coverage gaps -- corrupt-record MISS, coinbase-only-record BAN -- closed with new tests). One LOW deferred to 2.2.3 itself, not actionable yet: on any `ReadBodyAt` failure mid-range, truncate the response at the last good body, never leave a null `CTransactionRef` in `vBodies`. 3 new tests, full mutation re-verification (7 mutants across F-146+F-147, all die or are shown structurally inert for a stated reason) on a genuine clean rebuild. Full suite: 519 cases green (`!miner_tests`, up from 516); `miner_tests` alone unchanged (the one known pre-existing failure). **A second Fable review, of F-147's own fix, found F-147 2026-09-22 (F-148)**: its central "harmless by construction" safety claim about ignoring `LookupServeableBodyPositionByHash`'s return value held only for `bodyrange.cpp`'s one current caller, which always passes a fresh `FlatFilePos` -- simulated the obvious next caller (2.2.3's serving loop reusing one `FlatFilePos` across requests) directly and confirmed a prior successful lookup's real position would survive into a later miss under that mutant. Fixed by making the null guarantee unconditional: `LookupServeableBodyPositionByHash` now calls `posOut.SetNull()` on every failure path, not merely leaving it unwritten, with two new tests that deliberately pre-load `posOut` dirty before calling (the prior test only proved this against an already-null fresh variable, trivially true either way). Also fixed the live-datadir bug class (F-127, F-147) at its root rather than per-file: `BasicTestingSetup` (`test/test_raptoreum.cpp`) already computed a private, destructor-cleaned temp root for every instance but never pointed `-datadir` at it -- only the heavier `TestingSetup` did; moved those two calls into `BasicTestingSetup` itself, deleted F-147's now-fully-redundant `BodyRangeTestingSetup`, trimmed the same now-redundant calls from `BodyStoreTestingSetup`. A third file inheriting `BasicTestingSetup` directly can no longer reintroduce this bug. Independently re-swept for siblings rather than trusting the reviewer's own sweep -- confirmed the only three files with real body-store I/O are the two already covered plus `acceptancebit_tests.cpp` (already on `TestingSetup`-derived isolation, never at risk). 2 LOW: the comment F-147 added claiming its round-trip assertions would catch a `SERIALIZE_METHODS` field-order swap was itself wrong (a pure reorder changes writer and reader identically, so a round-trip test can never observe it, verified directly) -- fixed with a golden-hex wire-format test instead, mutation-verified to actually catch that swap; the overflow-BAN check's exact non-overflowing boundary (`nStartIndex + nCount == UINT32_MAX`) was untested, letting a `>`-to-`>=` off-by-one survive -- fixed with a boundary test, mutation-verified. All four fixes individually mutation-verified on a genuine clean rebuild. Full suite: 522 cases green (`!miner_tests`, up from 519); `miner_tests` unchanged; live datadir's stray file mtime unchanged throughout, reconfirmed. **2.2.3a built 2026-09-22 (F-149)** -- the serving handler, this tree's first off-`cs_main` block-serving path: `net_processing.cpp` gains a `GETBODYRANGE` dispatch arm and a `SendBodyRange` helper (`SendBlockTransactions`'s own precedent), calling the new pure `BuildBodyRangeResponse` (`bodyrange.h`/`.cpp`) which chunks the response to a byte ceiling (`DEFAULT_MAX_BODYRANGE_BYTES`, 1 MiB, two `static_assert`s tying it and `MAX_STANDARD_TX_SIZE` to `MAX_PROTOCOL_MESSAGE_LENGTH`) -- `cs_main` touched only for the brief, in-memory `Misbehaving()` call on `BAN` (confirmed `Misbehaving` itself requires it; confirmed `PushMessage` does not). **Explicitly deferred, not built here**: the announcer ring (misbehaviour tracking for who owes an answer), the per-connection budget/`-maxuploadtarget` integration, the `-maxbodyrangebytes` runtime knob (a fixed compiled default is used instead), and the hash-mismatch/over-delivery/unsolicited-late-response rules (those check an INCOMING response, i.e. 2.2.4's fetching side, not this handler). The announcer ring's own open dependency from F-143 (its depth tied to "this project's still-unfixed minimum body-retention floor") was resolved by research rather than left open: `MIN_BLOCKS_TO_KEEP`/`NODE_NETWORK_LIMITED_MIN_BLOCKS` (both 288) already govern exactly this question, for pruning and for an analogous peer-trust bound already in `net_processing.cpp`, respectively -- recorded as the answer for whoever builds the ring next. TDD: `BuildBodyRangeResponse` written test-first (confirmed RED via non-existence), 6 new `bodyrange_tests.cpp` cases. Mutation-tested, 5 mutants: 4 died immediately; the 5th (turning a `ReadBodyAt`-failure `break` into `continue`) survived every content-based assertion -- the final response comes out identical either way once reads start failing, so the only difference is HOW MANY doomed reads are attempted, and with `nCount` bounded only by the overflow check (up to `UINT32_MAX`), this was a genuine DoS gap in the author's own first draft: reproduced directly, the mutant provably hung past 30 seconds. Closed with a timing-bounded test (5-second budget against a normally-microsecond operation). Full suite: 528 cases green (`!miner_tests`, up from 522); `miner_tests` unchanged. `net_processing.cpp`'s own dispatch arm has no unit coverage, matching this tree's already-accepted convention (`SENDCMPCT` the same way) -- all response-building LOGIC lives in the fully-tested `bodyrange.cpp` instead. **Reviewed 2026-09-22 (F-150)**: 1 HIGH -- a genuine, measured DoS bug in 2.2.3a's own shipped code, not deferred scope: `ReadBodyAt` (built in an earlier phase) re-reads a record's whole offset table on every call, so `BuildBodyRangeResponse` calling it once per body made ONE range request cost ~12s of real CPU on `ThreadMessageHandler` against a 100,000-body record -- the same thread that also dispatches LLMQ DKG/ChainLock messages. Fixed with a new `ReadBodyRange` store primitive (`bodystore.h`/`.cpp`) that opens the record and reads its header exactly once regardless of how many bodies are read; `BuildBodyRangeResponse` now delegates to it, same black-box contract, O(record size) instead of O(nCount × record size). 1 MEDIUM, confirmed real but judged defensible with a hard gate, not silently accepted: no per-message-type rate limit anywhere in `net_processing.cpp` protects `GETBODYRANGE` (`GETBLOCKTXN`'s own `MAX_BLOCKTXN_DEPTH` has no analogue here) -- **2.2.3a must not run on a live/exposed network until the per-connection budget (still F-143's own deferred scope item) lands; this gate is recorded directly in `net_processing.cpp`'s own `DEFAULT_MAX_BODYRANGE_BYTES` doc comment**, not just here. 2 LOW: F-149's own DoS-guard test would have hung, not failed cleanly, against a reintroduced quadratic bug at its `UINT32_MAX` scale -- the real, properly-bounded regression guard now lives in `bodystore_tests.cpp` (a genuine 50,000-body reproduction); the ceiling check's exact-landing boundary was untested, closed with a mutation-verified test. Full mutation re-verification (5 new mutants, all die; F-149's own 6 `BuildBodyRangeResponse` tests still pass unchanged through the new delegation) on a genuine clean rebuild. Full suite: 535 cases green (`!miner_tests`, up from 528); `miner_tests` unchanged. **A third, targeted Fable review converged 2026-09-23 (F-151)**: no HIGH, no correctness bug -- the signal this project uses to stop iterating on a piece. 1 MEDIUM: `ReadBodyRange`'s F-150 fix still read the record's WHOLE offset table for its header regardless of how few bodies were requested, so a single-body request cost the same ~5.2ms as a huge one (measured against a real 700,000-body record, independent of which body was asked for) -- fixed to read only the one offset entry actually needed, O(1) header cost instead of O(record size); calibrated, mutation-verified regression test (~78x measured gap between correct and reintroduced-bug timings on the build machine). 1 MEDIUM, upgraded from documented to enforced: a doc-comment-only deployment gate has no failure mode if ignored, so **2.2.3a is now gated by an executable, default-off `-servebodyrange` flag** (`init.cpp`/`net_processing.cpp`, matching this file's own `-perfwithhold*` precedent for an unfinished feature) -- the handler silently declines every `GETBODYRANGE` request until an operator explicitly opts in, not merely documented as unsafe to run. 2 LOW: a corrupt-body-that-decodes-without-throwing edge case found and explicitly recorded as an accepted, deferred gap (this store has no corruption-inducing path today, and the real defence is the requester-side per-body hash check, 2.2.4's job); an untested documented branch closed with a one-line test. Full suite: 535 cases green (`!miner_tests`, test count unchanged from F-150 -- existing tests augmented, one new one-line test added); `miner_tests` unchanged. **2.2.3a is built, reviewed twice, and considered settled: deployable only with `-servebodyrange` explicitly set, and only alongside the per-connection budget once 2.2.3b exists.** **2.2.3b built 2026-09-23 (F-152)** -- the announcer ring itself: new `CAnnouncerRing` (`announcerring.h`/`.cpp`, pure and fully TDD-tested, 8 tests) records which blocks a peer announced, height-bounded (`ANNOUNCER_RING_DEPTH` = `NODE_NETWORK_LIMITED_MIN_BLOCKS`+2 = 290, resolving F-143's own left-open depth dependency) rather than count-bounded (F-143's round-3 finding: count-bounding is gameable). Wired into `net_processing.cpp` at the insertion points F-143's own 5-round spec review converged on -- `UpdateBlockAvailability`/`ProcessBlockAvailability`'s two genuinely-distinct hash-resolution branches (confirmed directly against source, not assumed), plus a new per-header loop in `ProcessHeadersMessage` closing a gap reproduced directly before fixing: today's tree only records a HEADERS batch's own TRAILING header, on both its success path and (via `ChainstateManager::ProcessNewBlockHeaders`'s own confirmed incremental out-param population) its partial-failure path. INV and the unconnecting-headers deferred-resolution path need no insertion of their own -- both already route through the two functions above. Scope boundary: this is the record-and-query mechanism only; nothing yet consumes it (`WasAnnounced` unused) -- that is 2.2.4's job, once the fetching/retry logic that would actually act on it exists. `net_processing.cpp`'s own three-call-site wiring has no unit coverage (matching this tree's established convention); the ring's own record/evict/query logic is fully tested. Full suite: 543 cases green (`!miner_tests`, up from 535). **Reviewed 2026-09-23 (F-153)**: 1 HIGH -- the ring's own "height-bounded" eviction used the CONNECTED chain tip as its reference, but during headers-first sync that lags far behind the headers actually being recorded for a fast peer, so every far-ahead entry was kept forever and `Record`'s own O(ring size) eviction scan made this quadratic. Independently reproduced before fixing: 692s for 200,000 `Record` calls at real mainnet scale. Fixed with `EffectiveHeight` = `max(currentTipHeight, highest height ever recorded)` as the eviction/freshness basis -- re-reproduced after the fix: 81ms at the same scale (~8,500x), size correctly bounded regardless of input size, degrading back to the original behaviour once sync is caught up. 1 MEDIUM: `CMPCTBLOCK` announcing a block with an unknown parent was a real sixth announcement path the "closed set" framing missed (lower severity since that peer also never becomes a download source on the strength of it) -- fixed by mirroring the HEADERS-unconnecting pattern. 1 MEDIUM: two boundary mutants on the ring's own eviction cutoff survived the original tests (memory-hygiene off-by-ones, not correctness bugs -- `WasAnnounced`'s own re-derived-freshness property held regardless) -- closed with two boundary tests. 2 LOW: defensive hardening for a structurally-unreachable loop-termination case; doc corrections (the insertion-point set is "closed as of 2.2.3b," not permanently, and the ring dies with its peer's connection on disconnect). Full suite: 546 cases green (`!miner_tests`, up from 543). **A second, targeted review converged 2026-09-23 (F-154)**: no HIGH, no remaining bug. The one MEDIUM is an honest limitation the HIGH fix's own correctness creates, judged inherent and documented rather than patched: **during any long catch-up (IBD, or a long reconnect), the ring provides close to no accountability for fetches near where the node's own progress actually is** -- the memory-bounding fix windows around whichever is higher, the connected tip or the highest height a given peer has ever announced, and during headers-first sync a peer's own header stream routinely races far ahead of body-fetch progress, evicting near-tip entries long before a fetch failure could consult them. `announcerring.h`'s own comment now states this explicitly, with a concrete worked scenario, and instructs that 2.2.4's design must not assume ring coverage during a long catch-up. 2 LOW: the fix's own load-bearing update-before-use ordering was unguarded (closed with a new test); an invariant-violation log line was silent by default despite claiming to "log loudly" (switched to unconditional). Full suite: 547 cases green (`!miner_tests`, up from 546); `miner_tests` unchanged. **2.2.3b is now considered settled, matching the same two-round stop condition already applied to 2.2.3a -- no further review round without being asked again.** **2.2.4 built 2026-09-24 (F-155, F-156)** -- the single-source fetching client and the previously-unbuilt body-ARRIVAL write path, both delivered as originally scoped by this row's own opening line: `CChainState::ProcessFetchedBodyRange` (`validation.h`/`.cpp`) runs `MaterialiseBlock` (already existed, unwired until now) → the body-dependent `CheckBlock`/`ContextualCheckBlock` rows (`fCheckPOW`/`fCheckMerkleRoot` both `false`, provably redundant once `MaterialiseBlock`'s own per-tx hash check has run) → the existing `SaveBodyToDisk` → status bits (write-before-claim, F-133/F-135's crash-ordering discipline applied to this second call site) → both indexes (hash-keyed immediately via `RecordBodyPositionByHash`; height-keyed via the existing connect-time mechanism once the caller's `ActivateBestChain` runs). `net_processing.cpp` gained a gated (`-fetchbodyrange`, default off) `SendMessages` section issuing `CGetBodyRange` requests -- peer choice is single-source only via `CAnnouncerRing::WasAnnounced` (now wired to production for the first time; C1's multi-source scoring stays deferred to 2.4) -- and a `BODYRANGE` response dispatch arm. B3's real remaining gap closed exactly as scoped: `g_body_retry_state` re-keyed from a bare block hash to `(NodeId, hash)` (reusing its existing backoff arithmetic unchanged, factored into `NextBodyRetryBackoffMicros`, not reinvented), plus a new aggregate concurrent-chase cap (`nBodyRangeInFlight` vs. `-maxbodyrangeinflight`, default `DEFAULT_MAX_BODYRANGE_INFLIGHT`=16) -- not per-peer, matching this row's own scope note that per-peer budgeting is a separate, still-deferred concern. Two real bugs found and fixed before any commit: a `FinalizeNode` resource leak (a disconnecting peer's in-flight slot was never freed, permanently stranding that block's fetch and narrowing the cap) and a connected-but-silently-unresponsive-peer gap the disconnect-only fix didn't cover (closed with `IsBodyRangeRequestStale`, reusing the existing backoff ceiling as the staleness threshold, not new backoff logic). Full suite: 571 cases green (`!miner_tests`, up from 547); `miner_tests` unchanged. Left open for the next review round (see F-155's own D-156): `ProcessFetchedBodyRange` trusts its caller to supply a `commitments`/`pindexNew` pair that actually correspond to each other rather than cross-checking `commitments.GetHash()` itself -- safe today since the only real caller always sources `commitments` via `ReadCommitmentBlockFromDisk(pindex)`, but undefended if a second caller is ever added. **Reviewed 2026-09-24 (F-157)**: 1 CONFIRMED HIGH -- the `BODYRANGE` handler held `cs_main` across its own `ActivateBestChain` call, violating that function's documented (and, at this file's other two call sites, already-honoured) "must not be called with cs_main held" contract; fixed by scoping, and hardened with a Clang `LOCKS_EXCLUDED(cs_main)` annotation on both `ActivateBestChain` overloads (`validation.h`) so the contract is now checked at compile time under Clang, empirically RED/GREEN verified via `clang++ -fsyntax-only -Wthread-safety` since `DEBUG_LOCKORDER`'s own runtime assert is a no-op in this project's default build. 2 MEDIUM: `FindNextBlocksToDownload`'s staller-detection never consulted the new `vBodyBlocks` candidate list, risking a wrongful stall/disconnect against an innocent peer while genuine body-range work was outstanding for a DIFFERENT peer -- fixed with an extracted, tested `HasOutstandingBlockDownloadWork` predicate; `g_body_retry_state`'s own re-keying (F-155) introduced the same disconnect-leak class F-155/F-156 already closed for `mapBodyRangeInFlight` -- closed for its sibling map in `FinalizeNode`, with a new invariant assert exercised by the existing `denialofservice_tests.cpp` suite. 3 straightforward cleanups folded in (a shared "bodies wanted" helper, a shared in-flight-erase helper closing an invariant-drift risk, and throttling the stale-request reaper from once-per-peer-per-round to once-per-second). Full suite: 575 cases green (`!miner_tests`, up from F-156's 571); `miner_tests` unchanged. **Reviewed by an independent, different-model second reviewer, 2026-09-25 (F-158)**: found a CONFIRMED HIGH cross-peer ban misattribution (a multi-chunk fetch's `mapBodyRangePartial` accumulator had no per-chunk content validation, only wire-shape -- an attacker's early bad chunk could get an honest LATER peer banned for completing the range) -- fixed with per-chunk hash validation against the block's own committed identifiers as each chunk arrives, not just once at completion. Found and fixed a CONFIRMED MEDIUM real regression, verified by A/B functional test: `FindNextBlocksToDownload`'s 2.2.4 routing had UNCONDITIONALLY removed the pre-2.2.4 whole-block GETDATA re-request fallback for a commitment-known/body-missing block, so with `-fetchbodyrange` off (the DEFAULT) such a block became permanently unrecoverable -- restored as a fallback (`fFetchBodyRangeCapable`, gating on both the flag and the specific peer's `NODE_COMMITMENTS` support), including 1.3.6/H-2's own retry-backoff for that fallback path. `feature_body_refetch.py`/`feature_body_refetch_backoff.py` (existing functional tests, neither wired into `test_runner.py` before this fix -- exactly how the regression shipped past a green C++ suite) now both wired in and passing against a real built node. Also fixed: empty (coinbase-only) commitment-only blocks, previously never completed by any mechanism (wrongly assumed unreachable); `mapBodyRangePartial`'s own missing abandonment cleanup (its sibling maps got this in F-155/F-157). **Explicitly noted**: with the whole-block fallback now conditional, a capable peer/config still depends entirely on the announcer ring for recovery once a block routes to the GETBODYRANGE path -- F-154's own documented long-catch-up accountability gap applies fully here; **the fetch client is tip-path-only, not an IBD/long-catchup recovery mechanism.** Full suite: 583 cases green (`!miner_tests`, up from F-157's 575); `miner_tests` unchanged; both functional tests pass. The real per-connection BUDGET (as opposed to the ring, 2.2.3a's own still-open MEDIUM) remains unbuilt. | 4-6 w | rate-limit policy and multi-source scheduling unknown |
| 2.3 | **Relay cap re-index.** `InvBroadcastMax()` reads a throughput target instead of `MaxBlockSize()`. Mandatory at any target — the shipped cap delivers 50-65 tx/s per peer; 50,000 was measured to converge 1,500 tx/s to sixteen peers. **Built 2026-09-21 (F-138).** `INVENTORY_BROADCAST_MAX_PER_1MB_BLOCK * MaxBlockSize()/1e6` replaced with a flat `INVENTORY_BROADCAST_MAX = 50,000` (F-5's own measured value) -- the old formula's coupling to `MaxBlockSize()` meant the cap would have silently jumped ~14x the day `g_commitmentBudgetActive` activates (110 MB vs 8 MB), a side effect of an unrelated flag, not a throughput decision. The dead `_PER_1MB_BLOCK` constant is removed rather than left alongside the new one. **Necessary, not sufficient**: perf-results.md already found message-handling cost itself ceilings relay at ~900 tx/s once this constant is no longer the binding limit -- reaching 1,500 tx/s needs the relay path to get cheaper or concurrent, real work, not a constant, and remains unscheduled. Full suite green (476 cases, unchanged count -- a single constant substitution, verified by regression and inspection, not a dedicated unit test). | 1 d | **done** |
| 2.4 | **New-smartnode onboarding.** Multi-source body fetch plus verification of the store at rest, so one corrupt or deliberately erasing source cannot propagate its hole to every node that joins later. | 1-2 w | straightforward |

**2.2 is the tip critical path, not a history service.** A worst-case `BLOCKTXN` at the 2 MB
design point is 62,500 × F-30 = 23 MB (F-33), a sender above K-7's limit is disconnected, and nothing in the
compact-block path chunks (K-10). Any node ~8,400 transactions (~16 s) behind therefore loses compact
reconstruction, and the whole-block fallback carries no bodies under this format.

## Phase 3 — existing-code prerequisites · 4-5 weeks, independent

Interleave anywhere. 3.1 gates 2.2; the asset fixes want to land before activation regardless.

| # | Component | Effort | Design |
|---|---|---|---|
| 3.1 | **DIP8 signing-attempts process.** Deliberately skipped today, so competing blocks at one height split the quorum's signatures and no ChainLock forms. Decoupling makes that condition *schedulable* rather than a race, because identifiers arrive instantly and body release is the attacker's choice. **Gates 2.2.** **Built 2026-09-21 (F-136).** Neither this codebase nor Dash Core's own current `develop` had ever implemented DIP-0008's real spec (an incrementing `attemptNum` converging via a `GetMostSignedSession` query) -- confirmed by fetching both the spec text and Dash's real source directly. **Decided (owner): a minimal fix**, not the full spec -- retry with a time-slotted request id (`GetChainLockAttemptNumber`, 30s slots off `GetAdjustedTime()`, so every quorum member computes the same attempt number without a per-node counter desyncing them) instead of building `GetMostSignedSession`/`IsMajorityPossible` from scratch. A hard constraint found while designing, not optional: `ProcessNewChainLock`'s verification recomputes a request id from height alone and `CChainLockSig`'s wire format has no attempt-number field, so an attempt's own recovered signature could never be verified by a peer directly -- DIP-8's real two-round structure (attempts converge under a separate `clsig-attempt` prefix; one succeeding triggers a second, fixed-id round under the existing `clsig` prefix that produces the actual broadcastable CLSIG) is required, not an enhancement, and both rounds are built. The retry/finalization decision logic is four pure functions in `quorums_chainlocks.h` (`ShouldNegotiateCommitments`'s own pattern), letting the state machine be unit-tested (17 new tests, `chainlocks_tests.cpp`) without the live smartnode/BLS-quorum scaffolding this tree's own precedent already declined. Not a consensus change -- wire format and verification untouched, no activation gate needed. Mutation-tested (4/4 caught, including one that exactly reproduces the original bug). Full suite green (472 cases). Still open, deliberately: no finalization timeout (bounded/self-resolving, same severity as today's baseline if a round never converges); the full DIP-8 spec remains unbuilt anywhere, including Dash mainnet. **Reviewed 2026-09-21 (F-137), unusually rigorously** -- the reviewer built the real daemon and ran `test/functional/feature_llmq_chainlocks.py` end-to-end against it (passes), then analyzed real node logs. Found and fixed a genuine MEDIUM regression: aggregation+gossip latency can let a node's own scheduler roll into the NEXT attempt slot before its own attempt's recovered signature reaches it, discarding a genuine success as "stale" -- in the honest, no-attacker case this could get WORSE than the original bug (a stuck height) if enough nodes raced ahead the same way. Fixed with `CLSIG_ATTEMPT_LOOKBACK` (2 slots): a recovered signature for a recent past attempt is still recognised, msgHash-guarded so nothing wrong can ever finalize. Also fixed: a test-coverage gap (the hoisted-finalization-guard mutant survived all 17 original tests; new test closes it), a conflicting-CLSIG re-check missing before finalization, and `AsyncSignIfMember`'s ignored return on the finalization path. One LOW item (finalization's quorum selection uses the current tip rather than `lastSignedHeight`, widening a pre-existing, fails-safe window) recorded, not fixed. One MEDIUM recommendation (a real 3-of-3 functional-test scenario proving the literal retry path, which has never executed outside the pure-function unit tests) deferred -- it needs the Python functional-test framework, a different toolchain from everything else built in this project so far. Full suite green (476 cases). | 1-2 w | **done, reviewed (F-137)** |
| 3.2 | **Persist the round-voting count at connect.** `NodeRoundVoting` reads 720 blocks of bodies with an assert and its result gates consensus (R-17 retracts the claim that this sets the window floor). Persisting the per-block fact removes the rescan — and with it the only stated reason the window floor is 720, which then needs a new justification. | 3-5 d | straightforward |
| 3.3 | **ChainLocks `Cleanup` rewrite.** Replace the O(mempool) `GetTransaction` walk under `cs_main` + `mempool.cs` every 30 s — 1.9 s of stall at 3M entries, though R-25 says it is not what caps throughput — with event-driven pruning. | 1 w | straightforward |
| 3.4b | **Asset-cache follow-ups** — recovered from `archive/asset-cache-drag.md`, where they were the only copy: bound or evict the global in-memory cache, which grows without eviction from the `LoadAssets` floor; elide the copy for asset transactions too, since they still pay it in full; and narrow the residual surface, which after #481 is asset-typed transactions only. | 1 w | straightforward |
| 3.4 | **Asset fixes.** Intra-block mint visibility, undo keyed by `(assetId, tx index)`, and the distribution-type check that accepts any value. No longer fork-critical now asset state is uncommitted, still real divergences. | 1 w | straightforward |
| 3.5b | **Parallel-acceptance sizing**, recovered from `archive/mempoolaccept-port-analysis.md`: the behaviour-preserving Dash `MemPoolAccept` port alone is **1,000-1,500 lines**, which is the only sizing we have for phase 5's parallel work. | — | reference |
| 3.5 | **Already proposed upstream.** Per-ATMP asset-cache deep copy (#481) and the socket-handler busy loop (#480), both open against `develop`. | — | submitted |
| 3.6 | **Stale anchors.** `nMinimumChainWork` at block 421457, a `defaultAssumeValid` written with a letter `o` so it parses to zero, a top checkpoint at 394273. Low priority, but they are the floor under any eclipse argument. | 1 d | straightforward |

## Phase 4 — hardening, interfaces, activation · 13-18 weeks

| # | Component | Effort | Design |
|---|---|---|---|
| 4.1 | **Safety hardening.** One `HaveBodies` predicate plus an audit of every path that may materialise a block — which alone closes the 37-byte remote crash where the serving path checks "we have this block" then asserts on a failed read. Also: relay only what you can assemble; windowed nodes stop advertising `NODE_NETWORK`; `EnforceBestChainLock` gated on bodies with its `assert(false)` made graceful; the ChainLock safety walk flipped to fail closed; stall rules for body fetch; the serving privacy regression; and the paths that break without bodies — ZMQ `rawblock`, bloom-filtered blocks, block filters. **Gates 4.6.** | 3-4 w | straightforward |
| 4.2 | **Coverage telemetry.** Per-peer miss rates and per-*range* coverage, passive, shipped with the protocol. Honest loss is oldest-first, file-aligned, scattered, or a shrinking gap; deliberate erasure is contiguous, mid-history, aligned to nothing and stable. | 1 w | straightforward |
| 4.3 | **Index-build paths.** Every index is built by replay today. The asset index is a pure aggregate over unspent asset outputs, so it gets a build-from-coins pass — the `-reindex`-only route is dead on a windowed node. Wallet rescan becomes range-driven, and anything returning transaction contents returns an explicit "not held here". | 1 w | straightforward |
| 4.4 | **Mining and template.** Commitment-mode `getblocktemplate`: cache the template, drop the full `ConnectBlock` per poll (seconds of `cs_main` and tens of MB of JSON at 62,500 names). `submitblock` accepts both serializations. Mining hardware is untouched — the 80-byte header is byte-identical. | 3-4 w | in-node known; **external pool coordination is the schedule risk** |
| 4.5 | **Constants and policy.** `maxmempool` burst-sized (≥459 MB once K-12's gate is counted, against K-8's default), `-maxsigcachesize`, `BLOCKTXN` chunking, compact-block index widening (~7 lines — and 62,501 *fits* `uint16_t`, which is the trap), the relay in-flight constants, and a fee floor scaling with body bytes. | 1-2 w | straightforward |
| 4.6 | **Migration and activation.** One `UpdateManager` bit carrying format, cap and message length. Dual-format window, old nodes' unpriced serving obligation, the downgrade surface, the no-rollback question, and an activation criterion that reads 4.2's data rather than hashrate signalling, which cannot see coverage at all. | 4-6 w | partly unknown |

## Phase 5 — attested transaction class · 10-18 weeks, trí-gated

Kept live rather than deferred (owner, 2026-09-17). The throughput case for attestation is dead;
the structural one is not — a Spark job's result is not verifiable by anyone, so for that
transaction class a quorum signature is the only validity rule available. See §17 of the design.

| # | Component | Effort | Design |
|---|---|---|---|
| 5.1 | **Attestation ceiling, measured properly.** Quorum of K-13's live size with async threshold recovery (F-21) and the 100 ms `SendMessages` cadence addressed. Every existing figure comes from 3-13 member quorums. Needs 0.4. | 1-2 w | straightforward after 0.4 |
| 5.2 | **Batched attestation.** One threshold signature over a Merkle root of N items, partial-conflict semantics, the mining-gate interaction. The gate for everything attestation-based: one lock costs ≥1.15 ms of BLS (F-22) against ~118 µs of ECDSA saved (F-11), so break-even is 10-20 transactions per signature. Shared with InstantSend batching — build once. | 3-5 w | partly unknown |
| 5.3 | **Admission-only shortcut.** Acceptance skips script checks for attested transactions and does not cache; `ConnectBlock` verifies cold on the parallel queue. Consensus untouched, trust boundary is the one islocks already impose, a lying quorum cannot fork. The baseline any consensus version must beat. | 2-3 w | straightforward |
| 5.4 | **Attested transaction type.** A new `nType` whose validity rule is a quorum threshold signature over a declared result, plus the rules bounding what it may change. `ContextualCheckTransaction` whitelists types, so it is a fork either way. Blocked on three answers from trí: what is attested, what it may change, and whether ordinary payments are in scope. | 4-8 w | **unknown, scope not ours** |

## Measured as not needed — the descoping record

Recovered from `pre-decoupling-checklist.md` when it was archived. Each was once on the required
list and was disproven by measurement, so each is here to stop it being rediscovered.

| item | why it is off the list |
|---|---|
| **parallel validation / MemPoolAccept port** as a v1 blocker | acceptance is not the binding constraint (F-7); and the naive version is *slower* than stock (F-10) |
| **relay ordering rewrite** (the O(backlog) heap) | `-perfinvnosort` moved throughput <1% at the operating point; the "~40% of msghand" figure came from a 300k backlog at cap 50k-100k, a regime the target never reaches |
| **`getblocktemplate`**'s full `ConnectBlock` per poll | decoupling dissolves the hard part — a commitment block's bodies were validated at acceptance. Revisit trigger: sustained mining at scale (and see 4.4) |
| **message-size raises** as separate work | the send loop already chunks at `MAX_INV_SZ`; no splitting needed. The `MAX_PROTOCOL_MESSAGE_LENGTH` raise is still required, as part of 1.4 |
| **`dbcache` / `maxsigcachesize`** as acceptance fixes | swept at saturation with no effect across 13× and 16× raises, application confirmed in the node log; the ceiling is real compute. **Note:** `-maxsigcachesize` *is* required, for a different reason — K-4 holds only 2.1 blocks at 8 MB (4.5) |

## Gates

Four, and each carries real information:

- **0.1 → 1.3.** The probe kills or confirms the acceptance layer in a week instead of two months.
- **0.4 → 5.1.** No quorum measurement is meaningful until smartnodes run on the swarm.
- **3.1 → 2.2.** The fetch protocol is what makes a quorum split schedulable, so the process that
  lets a quorum converge has to exist first. **Satisfied 2026-09-21 (F-136)** -- 2.2 is now unblocked.
- **4.1 → 4.6.** Hardening gates activation rather than trailing it.

## Deferred, each with a tripwire

| deferred | brought back by |
|---|---|
| **Retention, sharding, challenges, repair** | sustained load above F-32's 80 tx/s, or growth crossing 1 TB per node per year (K-14) |
| **State root and snapshot sync** — and §7A.6's canonical asset element form with it, which was the hardest unsolved design problem | replay bootstrap becoming impractical, or the Smartnode tier thinning under storage pressure |
| **Attributable attestation shares** | ships with retention — a challenge can only penalise a party that promised something, and a recovered threshold signature names nobody |
| **Relay structural work and parallel acceptance — as blockers** | bursts that stop draining. A 60 s burst at 2,083 tx/s leaves ~71,000 transactions of backlog, clearing in ~80 s at the measured 900 tx/s of delivery |

> **InstantSend batching (5.2) was on that list and should not have been.** Under D-7 the design
> assumes InstantSend on, and mempool signing at the design point then needs roughly 27× the measured
> capacity (F-18) — so 5.2 is a **prerequisite of the design point**, not a deferral. Deferring it
> would only be coherent under a decision to ship without InstantSend, which nobody has taken.
> It keeps its number because that is where its design work sits.
| **Bounded prefill** | nothing depends on it; a latency optimisation |

## Assumptions this plan rests on

**X-1 — the target is peak, not sustained.** At the 512 GB Smartnode specified in RTM's contract
paper, the practical sustained ceiling is ~10-30 tx/s depending on replacement cycle (K-14, F-32). Tripping it
brings back retention first, then the state root.

**X-2 — Smartnodes hold the entire history.** This is what keeps replay bootstrap available and
the state root optional.

**Caveat on both.** `vExtraPayload` runs to K-11's 10,000 B, so F-30's 373-byte
body figure behind every storage and bandwidth number here could be off by up to 27× once the
contract layer's new transaction type is specified.

> **SCOPE, standing (owner, 2026-09-18).** Mainnet's current spork configuration is
> **deliberate and temporary**: InstantSend is switched off on purpose and **will be switched
> on**, and the quorum layer is broken and **will be fixed before decoupling**. Neither is this
> project's scope. **Design for sporks 2, 3 and 19 all on, mempool signing enabled, and healthy
> quorums** — the measurement below records what is *currently observable*, not what to design
> against, and nothing in this plan may be descoped on the strength of it.

**Answered by 0.3 on 2026-09-18** (mainnet spork state read from a production node; see
`perf-results.md`). Sporks **2 and 19 are ON, 3 is OFF**, and InstantSend mempool signing is off
because spork 2 carries a timestamp rather than `0`. Three costs are therefore **not measurable on
mainnet today** — the ChainLock safety walk is skipped, there is no ten-minute mining gate, and
no islocks are produced outside the retroactive path — but per the scope note above, **all three
are real for the design** and stay fully priced:

| cost | why it binds under the designed configuration |
|---|---|
| **every ChainLock-signing smartnode must converge** | with spork 3 on, `TrySignChainTip` refuses to sign a tip holding any transaction that is not islocked and is younger than 600 s, and `txAge` is 0 when the txid is absent entirely. Mainnet rotates a 200/400-member quorum, so in practice that is every smartnode. |
| **`maxmempool` sized with the mining gate** | ≥459 MB at the design point once `IsTxSafeForMining` holds unlocked transactions for K-12's ten minutes, against K-8's default |
| **per-node islock BLS verification** | `ProcessPendingInstantSendLocks` gates on `IsInstantSendEnabled` only, with no smartnode check — ≥1.15 ms per message, 0.6 of a core at 520 tx/s and 2.4 cores at 2,083, on the single `rtm-isman` thread |

**So 5.2 (batched attestation) is a prerequisite of the design point, not a phase-5 option.**
Mempool signing at the design point needs roughly 27× the measured capacity (F-18), so the batching that
5.2 builds is what makes InstantSend survivable anywhere on D-4's range. It stays
numbered in phase 5 because that is where its design work sits, but it is required, not
conditional.

0.3 also observed that ChainLocks have not formed in ~14 months (best chainlock 1,122,354 against
a tip of 1,432,200). **That is the known broken quorum layer, it is out of scope here, and RTM
will fix it before decoupling.** It is recorded only because it is why the quorum-dependent
figures above cannot be measured on mainnet today — which is what 0.4's swarm bring-up exists to
work around.
